# Upgrade Delta Map — noita-alchemists-descent.html

## Current TS port status

- Cell ids remain append-only. The three existing elixirs occupy 21-23, so the
  imported `alchemists-descent` materials are remapped as documented below:
  Toxic=24, Healium=25, Teleportium=26, Snow=27, Coal=28, Crystal=29,
  Fungus=30, Glass=31, Ash=32, Glowshroom=33. Cave Moss is 34 and
  `CELL_COUNT` is 35.
- Sandbox and Builder now use restored pixel icons for potions/elixirs and the
  remapped exotic materials. Builder material popovers use the same icon source
  as the toolbar.
- Sandbox exposes all 15 tactical spells in the reference order: Spark Bolt,
  Scatter Hex, Cast Bomb, Chain Lightning, Flamethrower, Ember Storm, Vitriol
  Spray, Frost Shard, Ice Lance, Seeker Wisp, Excavate Ray, Conjure Stone, Warp
  Bolt, Meteor, and Black Hole. Play-mode wand/card design remains the long-term
  combat direction; the fixed tactical list is the live Sandbox spell surface.
- The old minimal fortress button has been replaced by `src/world/fortress.ts`,
  a large real-cell prefab with curtain walls, towers, keep rooms, ladders,
  glass windows, a portcullis, torches, volatile stores, elixirs, toxic/
  teleportium shelves, vault materials, and moat/bridge details.

## core-elements
### Element ID expansion: 10 new cell types (IDs 21-30) [overlaps-ours] (L462-466)
TOXIC=21, HEALIUM=22, TELEPORTIUM=23, SNOW=24, COAL=25, CRYSTAL=26, FUNGUS=27, GLASS=28, ASH=29, GLOWSHROOM=30 appended after EMBER=20. World dims unchanged (WIDTH 1600, HEIGHT 1064, VIEW 525x357, line 459-460).
**REC:** IDs 21/22/23 COLLIDE with our append-only ABI (src/sim/CellType.ts: ElixirLife=21, ElixirLevity=22, ElixirStone=23). On port, remap ALL ten: TOXIC->24, HEALIUM->25 (or merge into ElixirLife, see handleExoticLiquid entry), TELEPORTIUM->26, SNOW->27, COAL->28, CRYSTAL->29, FUNGUS->30, GLASS->31, ASH->32, GLOWSHROOM->33, and bump CELL_COUNT. Every numeric reference in the new file's reaction code must go through this remap table.

### MATERIAL_PARAMS: 11 new entries [new] (L489-499)
STONE {name only — original had no STONE entry, makes it inspectable}; TOXIC {flowRate 0.50, flammability 0.30, bloomWeight 0.20}; HEALIUM {flowRate 0.55, evaporationSpeed 0.004, bloomWeight 0.35}; TELEPORTIUM {flowRate 0.60, bloomWeight 0.40}; SNOW {friction 0.30, meltChance 0.45}; COAL {friction 0.45, burnDuration 170, igniteChance 0.08}; CRYSTAL {resonance 0.80 — DEAD PARAM, never read anywhere in the file, bloomWeight 0.45}; GLOWSHROOM {flammability 0.30, bloomWeight 0.40}; FUNGUS {flammability 0.50, spreadRate 0.10, bloomWeight 0.30}; GLASS {name only}; ASH {friction 0.20}. All 20 legacy entries byte-identical to original L335-356.
**REC:** Port to src/config/params.ts with remapped IDs. Skip `resonance` entirely — DESIGN.md explicitly cut 'Crystal charge-novas'; the dead param is the vestige.

### Classification predicate extensions [new] (L711-716)
isSolid += CRYSTAL, GLASS, FUNGUS, GLOWSHROOM (they bear weight, anchor vines, pool steam under them). isConductor += TOXIC (electrified sludge pools; lightning/charge propagate through it). isLiquid += TOXIC, HEALIUM, TELEPORTIUM. blocksEntity += SNOW, COAL, ASH (so the <5-cell loose-rubble cluster rule applies to them). isGas unchanged.
**REC:** Port as-is into CellType.ts predicates (with remapped IDs). Note our isLiquid already includes the three elixirs at 21/22/23 — additions are additive, no conflicts beyond the ID remap.

### SPELL_PARAMS: 8 new spells + unlock progression [overlaps-ours] (L504-523)
New entries (legacy 7 unchanged): scatter 'Scatter Shot' {pellets 5, velocityForce 9.5, spread 0.42, explosionRadius 6, manaCost 15, cooldown 20}; acid 'Vitriol Spray' {spread 0.30, manaCost 1.7, cooldown 0 — a stream spell like flamethrower}; ice 'Frost Shard' {velocityForce 11, freezeRadius 7, damage 16, manaCost 16, cooldown 16}; wisp 'Seeking Wisp' {velocityForce 4.5, damage 13, manaCost 8, cooldown 8 — homing}; wall 'Conjure Stone' {radius 6, range 130, manaCost 18, cooldown 24}; embers 'Ember Storm' {count 16, manaCost 18, cooldown 30}; icelance 'Ice Lance' {velocityForce 16, damage 30, freezeFrames 150, manaCost 22, cooldown 35}; meteor 'Meteor Call' {velocityForce 6.5, explosionRadius 40, manaCost 70, cooldown 140}. ALL_SPELLS = 15-element hotbar order; START_SPELLS = ['bolt','bomb','flame','dig'] (the rest are learned mid-run — playLearn SFX exists); SPELL_ORDER aliased to ALL_SPELLS.
**REC (superseded by current TS port status above):** Keep the wand/card system as the long-term play-mode combat direction, but the fixed 15-spell roster is now intentionally ported as the live Sandbox tactical-spell surface and build-mode test surface. The new payload numbers remain useful card baselines for Vitriol Spray, Frost Shard, Seeker Wisp, Conjure Stone, Ember Storm, Ice Lance, and Meteor; Scatter is also live in Sandbox as Scatter Hex.

### Descent run-state globals (declarations only in this range) [overlaps-ours] (L543-556)
BIOME_SEQUENCE = ['earthen','fungal','frozen','flooded','timber','crystal','scorched','volcanic'] (8 biomes); levelState {depth, biome, locked, hasKey} — a key-and-locked-portal descent model; pickups[]; mechanisms[] (plates/levers/braziers/doors); portalObj; arteryPoints[] (loot placement along carved tunnels); guardSpawns[]; gamePaused; runeVaults[] (sealed strongrooms + rune switches); levelPopulated; runStarted. bombCharge relocated here (cosmetic).
**REC:** Overlaps src/game/Levels.ts (5-level persistent descent) and our waystone/secret/region-graph systems. Their model differs: 8 reshuffled biomes, key->locked portal progression, rune vaults; ours: fixed 5-level spine, breakable wells, persistence snapshots. Keep our descent skeleton (it is the design-doc canon); the implementing systems (mechanisms/levers/runes, lines >1561) belong to other agents' ranges — flag only that these state lists exist and that triggerExplosion already couples to two of them (see explosion entry).

### Audio: 10 new descent SFX presets [new] (L668-678)
playPickup (throttle 80ms, 660->880 sine + delayed 990->1320), playChest (3-note 392/523/784 triangle arpeggio via setTimeout 110/230ms), playKeyJingle (1568/2093/1760 sine, 70/150ms), playPortal (dual rising sines 110->440 + 165->660 over 0.8s + noise), playDoorGrind (0.35s noise 240Hz + 60->45 sawtooth), playLever (throttle 150ms, two square clicks), playDrink (3 descending-sweep sines 420/520/640 at 0/90/180ms), playShatter (throttle 100ms, highpass noise 3200 + 1900-2500->400 square ping), playLearn (4-note 523/659/784/1046 triangle fanfare at 0/140/280/420ms), playBrazier (0.3s noise 700Hz + 220->480 triangle whoosh). Engine core (ensureAudio/throttled/tone/noiseBurst) and all 11 legacy presets are byte-identical to original L434-498.
**REC:** Port as-is into src/audio/AudioEngine.ts. Hazard: setTimeout chains are wall-clock (won't pause with sim) — same pre-existing quirk as playCoin/playWaveHorn, keep consistent.

### Three.js render setup [cosmetic] (L680-708)
Byte-identical to original L500-528: WebGLRenderer 1050x714, ACESFilmic exposure 1.0, FloatType DataTexture over VIEW_W*VIEW_H*4 buffer, NearestFilter, quad overscan 1+4/VIEW, EffectComposer with UnrealBloomPass(1.5, 0.4, 1.0). Camera (563-585) and updateSimBounds M=44 (587-594) also identical.
**REC:** Skip — no changes; our Renderer.ts already covers this (plus our post-fx shaders which the new file lacks).

### Color factories for 10 new cells [overlaps-ours] (L748-757, 765-767)
getToxicColor [64-82, 118-144, 36-48] murky green; getHealiumColor [248-256, 110-140, 160-190] hot pink; getTeleportiumColor [150-180, 60-80, 235-255] violet; getSnowColor grey-white s=232-250 with +8 blue; getCoalColor near-black 30-44; getCrystalColor [96-146, 200-245, 228-255] cyan with correlated v channel; getFungusColor [40-60, 190-225, 150-180] teal-green; getGlassColor [185-205, 210-228, 222-238] pale; getAshColor warm grey a=95-117 with -4/-6 g/b; getGlowshroomColor [120-160, 230-255, 140-190] bright green. All registered in COLOR_FN.
**REC:** Port into src/sim/colors.ts as packed-RGB factories. For IDs 21/22/23 our elixirLifeColor/elixirLevityColor/elixirStoneColor already exist and must NOT be overwritten — new colors go to the remapped IDs. Note HEALIUM pink vs our ElixirLife color: if merging Healium into ElixirLife, pick one palette.

### damagePlayer damage-cause tag [new] (L889 (and 1011))
updateFlyingParticles passes a 4th arg 'impact' for hostile debris hits; triggerExplosion passes 'explosion'. damagePlayer signature (line 2104, outside range) is now (amount, kx, ky, src). Other ranges use 'toxic' etc. Particle system otherwise byte-identical to original L649-716 (same homing 0.30 accel/0.92 damp/2.5px pickup, deposit-behind logic, FIRE life 18-36 / SMOKE 30-60 re-seed).
**REC:** Adopt: aligns exactly with our mandated 'deaths by cause' telemetry counter (DESIGN.md). Thread a cause string/enum through our Player.damage API.

### triggerExplosion: strikeRunes hook [new] (L919)
Every explosion calls strikeRunes(cx, cy, radius+4) right after playBoom — explosions can activate rune-vault switches (strikeRunes defined at 5303, outside range; also invoked from projectiles at 1785 and the dig ray at 2370).
**REC:** Port only when the rune-vault system itself is ported (another range). Architecturally: do NOT hard-call from explosion.ts — emit through Ctx/EventBus to avoid recreating the original's choke-point coupling.

### triggerExplosion: crystal shatter shards [new] (L930-936)
Inside the blast disc, a CRYSTAL cell has a 60% chance to launch a glowing shard particle: type CRYSTAL, color getCrystalColor(), life 110, glow 1.8, velocity ((dx/d)*2.4 + jitter ±0.8, (dy/d)*2.0 - 1.6 - rand). No `continue`: the cell STILL proceeds through normal destruction (22% generic debris roll, then 30% FIRE / 20% SMOKE / else EMPTY, 40% charge=4). Shards re-deposit as CRYSTAL cells where they land (standard particle deposit).
**REC:** Port as-is into src/sim/explosion.ts (gated on remapped CRYSTAL id). Pure spectacle + terrain scatter; cheap and grid-honest.

### triggerExplosion: rim heat fuses sand to glass [new] (L972-977)
In the rim-sweep annulus (d² > radius²*0.5 and ≤ (radius+4)²), SAND cells convert to GLASS at 40% chance (with `continue`, so fused cells skip the orphan-speck knock-loose logic). Creates glass craters around every blast in sandy terrain.
**REC:** Port as-is. Combined with handleSand fusing this makes GLASS an emergent, player-craftable material — fits 'Glass (sand+heat)' from our year-one materials list.

### triggerExplosion: concussion flips levers [new] (L987-992)
After the rim sweep, every mechanism of kind 'lever' within (radius+10) of the blast center gets flipLever(m) — 'explosions are valid puzzle inputs'. flipLever defined at 5185 (outside range); levers also flip from projectile proximity <22px at 5196.
**REC:** Port with the mechanism system (other range). Matches our sensor-framework philosophy (sensors read raw sim events); route via event, not direct call.

### handleSand: electric + lava glass fusing [new] (L1017-1032)
Two new pre-movement checks on SAND (handler also serves GOLD via dispatch — checks gated on currentType===SAND so GOLD is unaffected): (1) if chargeGrid > 6, 22% chance/tick -> GLASS (lightning strikes glass the sand; explosion residual charge=4 does NOT qualify); (2) outer 10% gate/tick, then scan 4 orthogonal neighbors — each LAVA neighbor 22% -> GLASS (net ~2.2%/tick/lava-contact). Fall/slide physics below is identical to original L803-813.
**REC:** Port as-is into src/sim/elements/powders.ts. The charge>6 threshold matters: lightning writes charge 8/20, electrical spread writes 4 — only direct strikes vitrify.

### handleWater: dilutes toxic sludge [new] (L1121-1130)
New pre-movement check: 3% gate/tick, scan 4 orthogonal neighbors, first TOXIC found becomes WATER (getWaterColor) and break. The diluting water cell is NOT consumed — water purifies toxic 1-for-0. Flow skeleton identical to original L893-903.
**REC:** Port as-is (liquids.ts). Gameplay-relevant: flooding is the counter-verb to toxic pools; also means toxic cannot survive submerged in water long-term.

### handleLava: 5 new contact reactions [new] (L1237-1242)
Added to the original WATER/ICE/fuel/BLOOD-SLIME ladder (orig L990-1011, those unchanged): SNOW -> STEAM life 30 (unconditional, no chance roll); ignition list WOOD|OIL|VINES extended with FUNGUS and GLOWSHROOM (FIRE life 35); COAL 15%/tick -> FIRE with life = COAL.burnDuration (170, long-burning hot spots); TOXIC 30%/tick -> SMOKE life 35; HEALIUM -> STEAM life 40 with hard-coded pink color [255,175,205] (unconditional). Lava itself is never consumed by any of these.
**REC:** Port as-is into liquids.ts handleLava. These belong in our planned reaction-matrix data table rather than the if-chain.

### handleFire: ash residue + 6 new fuel reactions [new] (L1414-1482)
(1) Burnout: when lifeGrid hits 0, 10% chance to become ASH instead of EMPTY — only if the cell below is non-EMPTY (supported), so ash forms on surfaces, not mid-air (orig always EMPTY). (2) New neighbor reactions appended to the original WOOD/VINES/OIL/GUNPOWDER/ICE/BLOOD/SLIME/WATER ladder (all originals unchanged, same asymmetric neighbor list [x+1],[x-1],[y+1],[x-1,y-1]): FUNGUS ignites at flammability 0.50 -> FIRE life 35, 50% spawnSmoke at fire's own cell; GLOWSHROOM at 0.30 -> life 40, 50% smoke; COAL at igniteChance 0.08 -> life 170 (=burnDuration, no smoke); TOXIC burns at flammability 0.30 -> FIRE life 50 with 70% spawnSmoke AT THE TARGET CELL (t.x,t.y — unlike all others which smoke at (x,y)); SNOW -> WATER unconditionally on contact; HEALIUM -> STEAM life 40 pink [255,175,205]. Ember-lift particle and upwardSpread identical to original.
**REC:** Port as-is into thermal.ts. Watch the TOXIC smoke-position quirk and the supported-cell condition on ash — both are deliberate (toxic fires smoke at the burn site; no floating ash motes).

### handleSnow (new powder) [new] (L1316-1334)
Melt: scan 4 orthogonal neighbors; FIRE|LAVA|EMBER neighbor -> 45% chance/tick (meltChance) self becomes WATER. Movement: light powder — pass predicate is EMPTY|gas ONLY (snow floats on every liquid, including water — it does NOT sink); falls straight down at 70% chance/tick, else diagonal slide both directions at friction 0.30. Also melted by direct FIRE contact via handleFire (->WATER) and vaporized by LAVA (->STEAM). No NITROGEN interaction. blocksEntity=true.
**REC:** Port as-is into powders.ts. Key spec: the floats-on-water behavior comes purely from the restrictive pass predicate, not density — preserve it. Natural fit for our D3 Frozen biome kit.

### handleCoal (new powder fuel) [new] (L1336-1353)
Ignition: FIRE|LAVA orthogonal neighbor -> 8% chance/tick (igniteChance) self becomes FIRE with life 170 + rand(0..39) (vs wood's 45 — coal burns ~4x longer). Movement: sand-like (passes EMPTY, non-LAVA liquids, gas — sinks in water), friction 0.45 BUT diagonal slide only checks ONE random direction (x+dir), no fallback to the other side like sand — coal piles steeper. blocksEntity=true, loose-rubble crushable.
**REC:** Port as-is (powders.ts). Keep the single-direction slide quirk — it's the only physics difference from sand besides ignition. Long-burn fuel for braziers/waystone-lighting fits our fire-lit-waystone design.

### handleAsh (new featherlight powder) [new] (L1355-1371)
Dissolution: WATER orthogonal neighbor -> 10% chance/tick self becomes EMPTY (washed away, water unchanged). Movement: floats on all liquids (pass = EMPTY|gas); drift: 35% chance to pick a lateral offset ±1 else 0, then tries (x+drift, y+1) at 55%, then straight down at 50%, then friction-0.20 single-direction diagonal slide — net effect is slow, wandering fall. Produced by fire burnout (10%, supported cells only). blocksEntity=true but friction 0.20 means it barely piles.
**REC:** Port as-is (powders.ts). Ash is on our year-one materials list; this is a complete, cheap implementation. The water-dissolution makes post-fire cleanup a flooding verb.

### handleFungus (new growth automaton) [new] (L1373-1396)
Glowcap colonies, vine-template energy budget on lifeGrid: 0 = fresh spore -> init 16 + rand(0..21); -1 = mature (inert, keeps glowing); >0 = spreading. Per tick (only when not gated): 10% spreadRate gate; pick 1 random direction from 6 (E, W, N, S, NE, NW — no downward diagonals); target must be EMPTY AND orthogonally touch a solid that is not FUNGUS (hugs walls/ceilings like moss, cannot self-scaffold into open air); child energy = max(2, parent - 2 - rand(0..2)). Parent energy decrements 1 EVERY active tick regardless of spread success; at <=0 becomes -1 mature. Dispatched in the SECONDARY sweep pass (line 3061, alongside ice/vines) gated on !movedGrid. FUNGUS is solid, flammable 0.50, lava-ignitable, acid-soluble, contributes light (palette 6196-6199, other range).
**REC:** Port as-is into a new elements module (or vines.ts sibling — same lifeGrid-sentinel template). Honors our 'every new cell uses an existing behavior template' rule. Note it spreads ~5x less far than vines (energy 16-37 vs 70-124) and needs no water.

### handleExoticLiquid: TOXIC / HEALIUM / TELEPORTIUM [overlaps-ours] (L1398-1412)
Shared handler: delegates to handleViscousLiquid (pass EMPTY|STEAM|SMOKE, flowRate from params: 0.50/0.55/0.60), then `if (grid[x][y] !== type) return` re-entry check (only run quirks if the cell didn't move this frame). Quirks: HEALIUM evaporates at 0.004/tick -> STEAM life 40 with pink color [255, 170-199, 200] (slow permanent loss — you can't hoard it); TELEPORTIUM 0.004/tick spawns a visual-only violet sparkle particle ([190,110,255], life 22, grav -0.01, glow 2.2), budget-gated at MAX_PARTICLES-60; TOXIC 0.0015/tick spawnSmoke. Entity contact effects are OUTSIDE this range (2201-2207: toxic 0.20 dmg/tick ×0.25 with toxicology perk; healium heals; teleportium teleports). TOXIC is also a conductor, burnable (flam 0.30), diluted by water, smoked by lava; it is the 'spitter' enemy's gore (2557).
**REC:** HEALIUM functionally duplicates our ElixirLife (healing liquid w/ contact effect) — recommend merging Healium's evaporation quirk + numbers into ElixirLife rather than adding a 4th healing liquid; decide palette. TOXIC and TELEPORTIUM are genuinely new — port at remapped IDs 24+/26+. The post-move re-entry check (`grid[x][y] !== type`) must be preserved when adapting to our Ctx handler signature.

### GLASS / CRYSTAL / GLOWSHROOM as static solids (no handlers) [new] (L712, 931, 973, 1022, 1238, 1445-1455 (reaction sites))
None of the three has a physics handler — they are inert solids defined entirely by predicates and other materials' reactions. GLASS: created from SAND by charge>6 (22%), lava contact (~2.2%/tick), or explosion rims (40%); destroyed by acid (corrodes to STEAM — not in acid's immune list) and explosions; blocks entities. CRYSTAL: shatters into glowing shards in explosions (60%), acid-soluble, emits light (palette line 6198), `resonance` param dead. GLOWSHROOM: flammable (0.30 fire, lava-ignitable), acid-soluble, emits light (6199), does NOT spread (contrast FUNGUS).
**REC:** Port as data: add the three IDs to isSolid + colors + the reaction sites listed above; no new handler files needed. Skip any temptation to implement `resonance` (explicit design cut).

### handleAcid: unchanged 10% acid->gold (regression vs our nerf) [overlaps-ours] (L1201-1225)
Byte-identical to original L964-988: corrodes any neighbor except EMPTY/ACID/STEAM/METAL/SMOKE at corrosiveSpeed 0.80, WALL/WOOD/STONE 10% -> GOLD, acid self-consumes. The new file did NOT take the acid->gold nerf; and because the immune list wasn't extended, acid dissolves ALL ten new materials (each -> STEAM life 25), including GLASS, CRYSTAL, COAL, SNOW, FUNGUS, GLOWSHROOM, and the three new liquids.
**REC:** Ours is better: keep our 3% + water-catalyst nerf (src/sim/elements/liquids.ts:223-232, DESIGN.md mandate). DO adopt their implicit rule that acid corrodes the new materials (acid as universal solvent is a deliberate tool), but review GLASS/CRYSTAL solubility against our hardness-table plans before locking it in.

### Unchanged-systems audit for this range [cosmetic] (L525-541, 557-667, 680-724, 770-909, 1043-1313, 1484-1559)
Verified byte-identical to original: grid allocation, camera lerp/idle-zoom/sim-bounds (M=44), audio engine core + 11 legacy SFX, Three.js pipeline, swapCells, stainCell/splatterStain (still only tints WALL/WOOD/STONE/ICE — new solids NOT stainable), hash2/valueNoise/bakeBackground (seed 1337), spawnFlyingParticle/burstParticles, handleGunpowder, handleVines, handleViscousLiquid, handleNitrogen (no SNOW/new-material interactions), handleOil, handleIce, handleEmber (does NOT ignite COAL/FUNGUS — only WOOD/VINES/OIL/GUNPOWDER), spawnSmoke, handleGas (colFunc still dead param), runHarvesterField, updateScoreUI. Explosion core %s unchanged (30/20 fire/smoke, 22% debris, 45% wall rim, 40% charge, damage radii 1.6x/1.5x, cap 42).
**REC:** No action — our ports of these systems remain authoritative. Boundary note: castLightning at 1562 (next range) gains a `mul = 1` damage-multiplier parameter.

**NOTES:** HAZARD #1 — cell-ID ABI collision: the new file's TOXIC=21/HEALIUM=22/TELEPORTIUM=23 sit exactly on our save-format-frozen ElixirLife/ElixirLevity/ElixirStone. The port uses one shared remap table: TOXIC->24, HEALIUM->25, TELEPORTIUM->26, SNOW->27, COAL->28, CRYSTAL->29, FUNGUS->30, GLASS->31, ASH->32, GLOWSHROOM->33, with Moss at 34 and CELL_COUNT=35. HAZARD #2 — triggerExplosion's coupling grew: it now reaches into runeVaults (strikeRunes) and mechanisms (flipLever) in addition to its original grid/particles/entities/audio/render set; in our architecture these must be EventBus emissions, not direct calls, or we recreate the original's choke-point cycle. HAZARD #3 — acid: the new file keeps the 10% acid->gold money printer our design explicitly nerfed (we shipped 3% + water catalyst); do not regress when merging acid's new-material solubility. Structurally surprising finds: (a) snow floats on ALL liquids purely via its restrictive pass predicate — easy to break if 'unified' with sand; (b) coal and ash slide in only ONE random diagonal direction per tick (no second-direction fallback like sand) — intentional steeper piling; (c) TOXIC fire smokes at the TARGET cell, all other fuels smoke at the fire's own cell; (d) fire burnout only leaves ash when the cell below is occupied (no floating ash); (e) handleExoticLiquid's post-move `grid[x][y] !== type` re-entry guard is load-bearing; (f) FUNGUS runs in the secondary sweep pass (line 3061, with ice/vines) gated on !movedGrid, not the main dispatcher at 3033-3036; (g) CRYSTAL's `resonance: 0.80` is declared but never read — matches our explicit 'crystal charge-novas' design cut, do not implement; (h) sand vitrifies only at charge>6 (lightning's 8/20), not from explosion residue (4) or electrical spread (4) — the threshold is the entire balance. Out-of-range pointers for other agents: new-liquid entity contact effects at 2201-2207 (toxic dmg 0.20/tick, toxicology perk 0.25x, teleportium touch-teleport), spitter enemy uses TOXIC gore (2557), lighting palette glow entries for new cells at 6196-6199, flask-fill registry at 6272-6274 confirms the three liquids integrate with a flask system overlapping our Flask v1. Files referenced: Y:/source/VibeCodingGames/Noita-sandbox/noita-alchemists-descent.html, Y:/source/VibeCodingGames/Noita-sandbox/noita-sandbox.html, Y:/source/VibeCodingGames/Noita-sandbox/src/sim/CellType.ts, Y:/source/VibeCodingGames/Noita-sandbox/src/config/params.ts, Y:/source/VibeCodingGames/Noita-sandbox/src/sim/elements/liquids.ts, Y:/source/VibeCodingGames/Noita-sandbox/src/combat/wands/cards.ts, Y:/source/VibeCodingGames/Noita-sandbox/src/entities/Player.ts.

## combat
### Chain lightning damage multiplier (mul param) [overlaps-ours] (L1562-1614)
castLightning(ox, oy, angle, mul = 1) — only change vs original L1193-1245 is the new 4th param; enemy strike deals params.damage * mul (lightning.damage still 28, range 340, branches 2 unchanged). mul is fed from dmgMul() at L2097-2102: berserk buff x1.6 (from thrown/drunk berserk flask, fxTimers.berserk) times power perk x1.25. Everything else (jittered ray +-0.35 blended 78/22, distSq<130 enemy hit, 5 scattered charge=8 cells, charge=20 + r4 explosion on terrain, arc life 8/branch life 6, shake 0.012 cap 0.045) is identical.
**REC:** Merge with our X: our Lightning.cast (src/combat/Lightning.ts) has no mul; our wand compiler applies dmgMul by re-casting the arc floor(dmgMul) times max 2 (WandSystem.ts L200-201). Adopt their scalar param — cleaner and composes with card dmgMul; keep our x4 compiler clamp and route both buff-mul and card-mul through the one param.

### Per-projectile damage multiplier (p.mul) [overlaps-ours] (L1722-1723)
updateProjectiles reads const mul = p.mul || 1 once per projectile; applied to bolt (18*mul), pellet (8*mul), iceshard (ice.damage 16 * mul), wisp (13*mul), icelance (30*mul). Set at cast sites (L2429 const mul = dmgMul()) — same berserk x1.6 / power-perk x1.25 source as lightning. Explosion radii are NOT scaled by mul.
**REC:** Adopt their field: our compiler already computes dmgMul (clamped x4) but applies it as stacked-bolt count because our Projectile contract has no damage field (WandSystem.ts L73-77 calls it 'heavy = more bolts v1'). Adding mul?: number to our Projectile type is the better design — replaces the count hack and lets buffs and cards multiply through one channel.

### Scatter Shot (pellet projectile) [new] (L1827-1837, 1875-1877)
New player projectile type 'pellet'. Cast (L2435-2440, outside range): 5 pellets per cast, each angle jitter +-0.21 rad (spread 0.42), velocity 9.5 * (0.85..1.15 random), life 70, NO gravity (absent from the per-type gravity table at 1744-1748 — flies straight). Enemy hit at distSq<120: damageEnemy 8*mul, knockback (vx*0.6, -1.0), then triggerExplosion radius 6 (SPELL_PARAMS.scatter.explosionRadius). Terrain hit: explosion radius 6. SPELL_PARAMS.scatter (L506): mana 15, cooldown 20. Render: light seed (0.3,0.7,0.9) at L3599.
**REC:** Port as-is into Projectiles.ts as a new ProjectileType + a 'scatter' spell card in cards.ts (5-projectile burst is one cast, distinct from our Twin/Triple multicast). Adopt their numbers.

### Frost Shard (iceshard) + freezeSplash + enemy chill [overlaps-ours] (L1745, 1838-1841, 1878-1880, 1929-1955)
New player projectile 'iceshard': gravity +0.04/frame, velocity 11, life 140, mana 16, cd 16 (SPELL_PARAMS.ice L511). Enemy hit distSq<120: damageEnemy 16*mul, knockback (vx*0.5,-0.8), sets e.chill=140 (consumed at L2694/2709 outside range: movement factor x0.45 while >0, decrements 1/frame; blue body tint at L3241), then freezeSplash at projectile pos. Terrain hit: freezeSplash only — no explosion, no terrain damage. freezeSplash(cx,cy,radius=ice.freezeRadius=7), NEW function L1930-1955: disc sweep — WATER becomes ICE unconditionally; EMPTY cells at 35% chance become ICE 'rime' only if 4-neighbor-adjacent to a solid (crusts exposed surfaces); both conversions skipped inside a player-protection box |X-player.x|<=5 AND player.y-18<=Y<=player.y+1 (never entombs the wizard); ends with 8 ice burstParticles (glow 1.6, grav 0.03) + playShatter() SFX (L676, new: throttled 100ms highpass noiseBurst 0.14/3200 + square tone ~1900-2500Hz).
**REC:** Merge with our status engine: we have the inverse (hostile 'frostbolt' freezing the PLAYER, Projectiles.ts L297-299 + L389-408, freezing <=6 water cells in r4) and sim-sampled status.frozen (nitrogen>=2 -> frozen 100, slowFactor 0.55, entities/status.ts). Port frost shard as a player card; map their e.chill onto a new chill field in EntityStatus (their chill x0.45 slow is softer than a hard freeze and stacks differently — keep both). freezeSplash's rime-on-surfaces and player-safe box are genuinely new — port the function, but parameterize the safe box from PLAYER_HALF_W/PLAYER_H (theirs hardcodes 5 and 18; ours are 4 and 17).

### Ice Lance (icelance) — piercing freeze projectile [new] (L1788-1810, 1856-1871)
New player projectile 'icelance': velocity 16, life 90, NO gravity, mana 22, cd 35, freezeFrames 150 (SPELL_PARAMS.icelance L517). PIERCES enemies: per movement substep, every enemy at distSq<130 with e.flash<=2 takes 30*mul + knockback (p.vx*0.4, -0.8), gets e.frozen=150, ice burst (10 particles [200,240,255] glow 1.8) + sine tone 900-1200Hz; projectile is NOT removed — the e.flash>2 guard (flash set by damageEnemy) prevents re-hitting the same enemy across substeps, so one lance can skewer multiple enemies once each. Hard-freeze consumption (L2695-2702, outside range): per frame vx*=0.78, attackCd held >=30 (cannot attack), jetFuel=0 (fliers drop), 4% frost-mote particle. Wake effect per substep: WATER in the 5x5 box around the lance freezes to ICE at 60%; 50% chance frosty contrail particle ([190,235,255], grav -0.005, glow 1.6). Terrain impact ('shatter'): erodeAt(gx,gy,3) — chews WALL/SAND/WOOD/ICE/VINES/STONE/GUNPOWDER in r3 disc, metal immune (erodeAt at L2377, outside range); freezes all WATER in disc dx^2+dy^2<=26 within an 11x11 box; 16 ICE-element debris particles (deposit real ICE cells on landing) + 8 white glow particles; triangle tone 1600Hz; removed.
**REC:** Port as-is as a premium card. Hazards: (1) the pierce guard couples to damageEnemy's flash constant — replicate explicitly (e.g. a hitSet on the projectile) instead of reusing flash; (2) their e.frozen semantics (hard freeze: attack lockout + jet kill) are STRONGER than our sim-sampled status.frozen (slowFactor 0.55) — extend our status consumption to add attack-lockout when frozen comes from a spell, or add a separate hardFrozen tier. Adopt their numbers.

### Seeking Wisp (wisp projectile) — homing [new] (L1749-1768, 1842-1844, 1881-1883)
New player projectile 'wisp': velocity 4.5, life 260 (long-lived), mana 8, cd 8 (cheap spam spell, L512). NO gravity. Homing: each frame finds nearest enemy within 240px (squared-dist scan vs e.y-5), accelerates 0.42/frame toward it, speed clamped to 5.2; flies straight if no target. Trail: every other frame, cyan particle [120,230,255] life 12, grav 0, glow 2.4. Enemy hit distSq<120: damageEnemy 13*mul, knockback (vx*0.5,-1.0) + triggerExplosion radius 5 (hardcoded). Terrain: explosion radius 5. Light seed (0.4,1.1,1.2) L3602.
**REC:** Port as-is BUT rename the projectile type — 'wisp' collides with our EnemyKind 'wisp' (src/core/types.ts L84). Use 'seeker' or 'seekerwisp' for the ProjectileType. Their homing should target our enemy list via ctx.enemies; numbers port directly.

### Meteor Call (meteor projectile) [new] (L1746, 1827-1831, 1845-1846, 1884-1886, 1912-1914, 1922-1925)
New player projectile 'meteor': velocity 6.5 with -1.0 initial vy lift (lobbed arc, cast L2511), gravity +0.07/frame, life 300, mana 70, cd 140 (ultimate-tier, L518). Enlarged enemy proximity fuse: distSq<200 (vs 120 for others); on enemy hit it does NO direct damageEnemy — just triggerExplosion radius 40 (explosion AoE handles damage; cf. bomb's 52). Terrain hit: explosion r40. NEW: airburst at life expiry (L1912-1914) — also explodes r40 when life hits 0 mid-flight (original projectiles except warp just vanished). Trail every frame (not %2): spawns a FIRE-element particle (life 16-25, grav -0.01, glow 2.4) — because element-typed particles re-deposit into the grid on landing, the meteor literally rains fire cells along its arc. Light seed (1.9,0.9,0.2).
**REC:** Port as-is. The FIRE-element trail relies on our Particles re-deposit rule (already ported, particles/Particles.ts) — verify deposited FIRE gets life seeded 18-36 as our deposit code does. Note the explosion-only damage model means dmgMul does not scale meteor at all — intentional, keep it.

### Thrown potion flask projectile (type 'flask') [overlaps-ours] (L1747, 1887-1889, 1910-1911)
Generic projectile integration for thrown brewed potions: gravity +0.16/frame, life 240; on terrain hit OR life expiry calls shatterFlask(p) (L6314, outside range): 8 GLASS-element debris particles (GLASS is a NEW material in this file), then liquid-filled types splashLiquid(fill, fillFn, radius 4); pure-effect brews (swift/berserk) burst sparkles and grant player.fxTimers.speed/berserk = 900 frames if the player is within 30px. Thrown at L6305-6308: vel 5.2 along aim + player.vx*0.5, vy -1.4 lift, carries p.flaskType from FLASK_TYPES (water/toxic/healium/teleportium/swift/berserk, L6276).
**REC:** Merge with our two systems: our Flask.ts is a MATERIAL siphon bottle (600-cell conserved inventory, own out-of-projectile-list bottle physics, expanding-ring spill that conserves exact cell counts) — strictly richer than their fixed-r4 splash, keep ours. Their flaskType potions map to our cauldron-brewed elixirs (cells 21/22/23). Adopt only the integration idea: route our thrown elixir bottles through the shared projectile list so black-hole gravity wells and swept collision apply to them. CELL ID HAZARD: their TOXIC=21/HEALIUM=22/TELEPORTIUM=23 collide with our IDs and GLASS is a new ID — remap per the append-only ABI.

### Hostile acid glob + generalized hostile-hit branch + damage-source tag [overlaps-ours] (L1748, 1812-1825)
New hostile projectile 'acidglob': gravity +0.12/frame, life 220, lobbed by an enemy at L2760 (arc velocity, vy -1.4). The hostile-vs-player branch (distSq<85 to player.y-9) is now type-switched: acidglob does damagePlayer(8, vx*1.3, -1.6, 'acid') + splashLiquid(ACID, r3) — deposits a real acid puddle ON the player's position; all other hostile types (fireball) keep the original 11 dmg + r10 explosion but now pass 'explosion' as a 4th damagePlayer arg. Terrain hit: splashLiquid(ACID, getAcidColor, 3). damagePlayer gained a src param (L2104) — damage-cause tag.
**REC:** Merge: we already extended the hostile branch ourselves for our mage's 'frostbolt' (Projectiles.ts L293-307, 6 dmg + status.frozen 120). Port acidglob alongside it as a third hostile type — fits our acidslime/mage roster. Adopt the src tag on playerCtl.damage: it directly serves the DESIGN.md telemetry mandate ('deaths by cause').

### splashLiquid helper [new] (L1957-1971)
New shared utility: splashLiquid(cx, cy, type, colorFn, radius) writes a filled disc of the given cell type into EMPTY/gas cells only (solids and liquids untouched), setting lifeGrid=0 per cell. Callers: acidglob player-hit (r3), acidglob terrain (r3), shatterFlask liquid fills (r4).
**REC:** Port as a World/sim utility — but ours should also zero the charge array (our Flask.pour does types/colors/life/charge; theirs leaves stale chargeGrid values, arguably a bug). Our Flask spill ring logic stays separate (it conserves counts; this is a fire-and-forget stamp).

### Rune-vault strike hook in projectile flight (strikeRunes) [overlaps-ours] (L1785)
Every NON-hostile projectile, on every movement substep, calls strikeRunes(p.x, p.y, 7) (L5303, outside range): scans runeVaults for inactive runes within r7, activates them (vault stone door dissolves 3 cells/frame bottom-up, toast banner, tone + shake + 18 green glow particles). Explosions also call it with radius+4 (L919). This makes any player shot a key for the rune secret system.
**REC:** Overlaps our secrets differently: we built probeHollow hollow-wall knocks on bolt/bomb/fireball TERRAIN impacts (Projectiles.ts L332-357) — a detection tell, not an unlock. The two coexist; if the rune-vault system (another agent's range) is ported, this is the hook point. Perf note: theirs runs per-substep per-projectile every frame — gate it to terrain/proximity events or early-out on empty vault list.

### Per-type gravity table [tuning] (L1743-1748)
Original had only bomb +0.14 / fireball +0.02. New adds: iceshard +0.04, meteor +0.07, flask +0.16, acidglob +0.12; pellet/icelance/wisp/bolt/warp get none (wisp gets steering instead). Our port added frostbolt +0.01.
**REC:** Adopt their numbers when porting each projectile; keep our frostbolt 0.01.

### Black hole / implosion / gravity wells / lightning arcs / electrical grid — unchanged [cosmetic] (L1616-1716, 1973-1998)
updateLightningArcs (1616-1621), playImplode (1624-1628), implosionCollapse (1630-1665), updateSingularityGravityWells (1667-1716), and updateElectricalGrid (1973-1998) are verbatim copies of original L1247-1347/L1447-1472 — every constant identical (event horizon max(9,(R*0.12)^2), 5% wall shear, 55% pull, enemy drag 0.22/contact dmg 2.2 at d<4, player drag 0.10, collapse R*1.2 sweep with 35% inward particles, inverted shockwave strength -16 speed 5.5, bloomKick +0.85 cap 1.1, charge spread targets incl. the asymmetric [x-1,y-1], decay 1+bleed, spread charge 4). Our TS port already has all of this 1:1 (Projectiles.ts L83-210, Lightning.ts, sim/electrical.ts).
**REC:** Skip — already ported faithfully; nothing to do.

**NOTES:** SCOPE: New file 1561-2000 = castLightning(1562)/updateLightningArcs(1616)/playImplode(1624)/implosionCollapse(1630)/updateSingularityGravityWells(1667)/updateProjectiles(1718)/freezeSplash(1930)/splashLiquid(1958)/updateElectricalGrid(1973). Net structural diff vs original 1192-1472: lightning gains a damage-mul param; black-hole and electrical systems are byte-identical; ALL real change is concentrated in updateProjectiles plus the two new helpers. ANSWER TO FOCUS QUESTION re 'conjured stone': SPELL_PARAMS.wall (Conjure Stone, radius 6, range 130, mana 18, cd 24, L514) and embers (Ember Storm, count 16, L516) and acid (Vitriol Spray, L510) exist but are NOT projectile types — no handling in this range; they are instant/spray casts implemented at the cast sites (~L2429-2511), which belong to another agent's range. HAZARDS FOR THE PORT FLEET: (1) projectile type 'wisp' collides with our EnemyKind 'wisp' — rename on port; (2) ice lance's multi-hit guard reuses e.flash>2 (set by damageEnemy) — fragile coupling, replace with an explicit per-projectile hit set; (3) freezeSplash hardcodes the player-safe box as halfW 5 / height 18 vs our PLAYER_HALF_W=4 / PLAYER_H=17 — derive from constants; (4) the new file has TWO freeze tiers as ad-hoc enemy fields (frozen = hard lockout 150f: vx*=0.78/frame, attackCd>=30, jetFuel=0; chill = 140f soft slow x0.45) while our port has one sim-sampled EntityStatus.frozen (slow x0.55) — merging needs a deliberate mapping (suggest: add status.chill; spell-sourced frozen also locks attacks); (5) shatterFlask spawns GLASS cells and FLASK_TYPES references toxic/healium/teleportium — cell-ID remap required (our 21/22/23 are taken; append GLASS after our last ID); (6) strikeRunes is called per-substep for every friendly projectile — O(runes x substeps x projectiles) every frame, gate it before porting; (7) splashLiquid does not clear chargeGrid on deposited cells (our pour does) — fix on port; (8) meteor's fire rain depends on the particle re-deposit rule seeding FIRE life — verify against our Particles deposit path; (9) damagePlayer grew a 4th 'src' arg ('acid'/'explosion') used file-wide — adopt for our telemetry counters; (10) the per-projectile p.mul (berserk x1.6, power perk x1.25) supersedes our WandSystem 'heavy = more bolts' workaround — adding mul to the Projectile contract retires that hack and should compose with the compiler's x4 clamp. Spell economy snapshot for the 5 new player projectiles: scatter 15 mana/20cd, wisp 8/8, ice 16/16, icelance 22/35, meteor 70/140 — a clean cheap-to-ultimate ladder worth keeping as card costs in Wave D terms. Legacy 7-spell params are unchanged from the original, so no retuning of already-ported spells is needed.

## player
### Player state extensions (owned-spell list, 3 flask slots, potion timers, perks, teleport cooldown) [overlaps-ours] (L2001-2017)
Player initializer gains: spells:[...START_SPELLS] where START_SPELLS=['bolt','bomb','flame','dig'] (L522) — ownership list consumed by the hotbar (L4248/4292), tome pickups push into it (L5427), shop sells unknowns (L5853/6121), mouse wheel cycles it (L6499-6502); flasks:[null,null,null] + flaskSel:0 (THREE flask slots vs our one); fxTimers:{speed:0,berserk:0} (flask-brew effects, set to 900-1400 frames at L6330/6353/6357); buffs:{stone:0,swift:0,levit:0,torch:0} (potion-pickup effects: stone 720f, swift 720f, levit 600f, torch 1500f set at L5458-5461); perks:{} (boolean flags set by Sanctum boons L6053-6064); tpCool:0 (teleportium cooldown). All timers tick down once per updatePlayer frame (L2176-2180). Rest of the initializer is identical to original L1475-1484.
**REC:** Merge into our PlayerState + status struct (src/entities/status.ts): fold fxTimers+buffs into the existing sim-sampled status struct as additional timers instead of keeping two parallel stores. Our flask is 1-slot by design ('1 slot first, 3 later' per DESIGN.md) — adopt their 3-slot belt as the planned upgrade path. spells[] ownership conflicts with our card economy: map tome pickups to card drops instead of porting the list.

### dmgMul() global player damage multiplier [new] (L2097-2102)
Returns 1 × 1.6 (if fxTimers.berserk>0) × 1.25 (if perks.power) = up to ×2.0. Attached as `mul` field on EVERY player projectile pushed in firePlayerSpell (bolt/pellet/icelance/iceshard/wisp/warp/meteor/blackhole), passed as 4th arg to castLightning (L2463), and on the mouseup bomb throw (L6534). Particle sprays (flame/acid/embers) do NOT get it. Consumed by projectile damage code outside this range.
**REC:** Port the berserk/power multipliers, but fold them into our wand cast-compiler's damage-multiplier pipeline (which already has the heavy charm ×1.7 and the ×4 clamp) rather than a standalone function — apply at compile/cast time so the ×4 clamp still governs.

### Typed damage sources + perk/buff resistances in damagePlayer [overlaps-ours] (L2104-2120)
damagePlayer gains a 4th param `src`. Multipliers applied in order: src==='explosion' && perks.blast → ×0.4; src==='fire' && perks.pyro → ×0.4; (src==='toxic'||src==='acid') && perks.toxicology → ×0.25; buffs.stone>0 (stoneskin) → ×0.4; then floor amount=max(0.5,amount). Knockback, invuln=30, screenShake +0.018 cap 0.05, blood burst min(16,5+amount*0.4) all unchanged from original L1565-1575. Note stoneskin does NOT cancel knockback here.
**REC:** Merge: our PlayerControl.damage() already has stoneskin (×0.5 + knockback cancelled + hitstop≥8dmg). Keep our stoneskin behavior (better feel: knockback cancel + hitstop), adopt their src-typing and the three perk resistances (blast 0.4 / pyro 0.4 / toxicology 0.25) and the 0.5 damage floor. Callers everywhere must start passing src — coordinate with explosion/enemy port agents.

### Game-over shows DEPTH instead of WAVE [cosmetic] (L2122-2132)
killPlayer is byte-identical to original except '#go-wave'.textContent = 'DEPTH ' + levelState.depth (levelState declared L545: {depth, biome, locked, hasKey}). Particles/SFX/shake identical.
**REC:** Skip — our playerDied event already exists; just emit depth from ctx.levels when in descent mode instead of wave number.

### Respawn economics: 40% gold tax, spawn-radius enemy thinning, longer invuln [overlaps-ours] (L2153-2172)
respawnPlayer changes vs original L1608-1619: invuln 120 (was 90); resets fxTimers.speed/berserk; gold tax: tax=floor(globalScore*0.4), subtracted, toast 'THE CAVES CLAIM N oz OF YOUR GOLD' (showToast L5083); enemies are FILTERED — only those within 200px of the spawn point are removed (dx²+dy²>200² kept) instead of enemies=[]; the waveState.active=false/intermission=90 reset is REMOVED (descent has no wave restart); adds updateHUD() call. findSpawnPoint (2134-2151) is unchanged.
**REC:** Ours is better and DESIGN-canonical: waystone-anchored respawn with 15% gold loss and world fully intact (src/entities/Player.ts respawn(), src/game/Levels.ts respawnPoint()). Skip the 40% tax. DO adopt their 200px enemy-thinning around the respawn anchor as an anti-death-loop guard (DESIGN's 'physics mulligans' spirit), and consider invuln 120. Note DESIGN says 15% should drop as a recoverable stain — neither implementation does that yet.

### New cell-contact effects: TOXIC damage, HEALIUM healing, TELEPORTIUM random teleport [new] (L2191-2228)
Body-sample loop (same 9-wide × 17-tall stride-2 grid, 45 samples max) extended: TOXIC(=21) adds 0.20 hazard DPS per sampled cell (×0.25 with toxicology perk); HEALIUM(=22) heals +0.14 hp per sampled cell per frame when hp<maxHp, and each touched cell has a 12% chance to be consumed (set EMPTY) — pink mote particle every 10th frame; TELEPORTIUM(=23) on any contact with tpCool<=0 triggers randomTeleport() (L6037-6050): sets tpCool=120, up to 60 random attempts at a free 4×17 pocket anywhere in the world (x 20..WIDTH-40, y 24..HEIGHT-60), zeroes velocity, violet bursts + rising tone. FIRE/LAVA hazard DPS now ×0.4 with pyro perk, ACID ×0.25 with toxicology — applied inline. Hazard damage still bypasses damagePlayer AND invuln (direct hp subtraction); the stoneskin buff therefore does NOT reduce hazard DPS. A hazardSrc variable is assigned ('fire'/'acid'/'toxic') but never read — dead code.
**REC:** Port, with MANDATORY cell-ID remap: their TOXIC=21/HEALIUM=22/TELEPORTIUM=23 collide with our ElixirLife=21/ElixirLevity=22/ElixirStone=23 (src/sim/CellType.ts, append-only ABI). Assign new IDs ≥24 in our enum and grow CELL_COUNT; the whole new-file block 21-30 (TOXIC,HEALIUM,TELEPORTIUM,SNOW=24,COAL=25,CRYSTAL=26,FUNGUS=27,GLASS=28,ASH=29,GLOWSHROOM=30) needs one project-wide remap table — coordinate across all port agents. Route the perk reductions through our sampleAndTickStatus so they also cover the status engine.

### Movement speed multiplier stack + swift jump [new] (L2182-2188, 2235-2237)
speedK = (fxTimers.speed>0 ? 1.45 : 1) × (perks.swift ? 1.18 : 1) × (buffs.swift>0 ? 1.5 : 1) — multiplicative, worst case ×2.57. Applied to BOTH accel (0.50×speedK) and maxRun (2.6×speedK). Jump velocity becomes -4.1 instead of -3.7 while buffs.swift>0. Friction 0.72, gravity 0.28/0.12, liquid damping 0.88, vy clamp (-4.6,5.0), sub-cell movement (halfW 4, h 17, stepUp 5), grounded probe — all identical to original.
**REC:** Adopt their numbers as a multiplier layer on top of OUR movement (src/entities/Player.ts), which has the DESIGN-mandated feel work the new file lacks (coyote 6f, jump buffer 8f, air-control accel 0.575, levitation ramp 0.40→0.62, landing feedback, statusSlow). Multiply speedK into both accel paths and maxRun; gate the -4.1 jump on the swift status timer.

### Levitation: free-levit buff + Featherweight perk [overlaps-ours] (L2238-2249)
Levitate branch now fires if player.levit>0 OR buffs.levit>0; drain only happens when buffs.levit<=0, at 1.15 × (perks.feather ? 0.55 : 1) per frame. Thrust -0.62/frame, regen 1.7 grounded/in-liquid, flame particles every 3rd frame — unchanged.
**REC:** Merge: our status.levity already implements free levitation (drain skipped while timer runs) and our levitation has the response curve (eased 0.40→0.62 thrust). Keep ours; add the feather perk as a 0.55 drain multiplier.

### Mana Crystal proximity aura + Mana Font perk [new] (L2252-2269)
Base mana regen 0.45 × (perks.manafont ? 1.6 : 1). Every 6th frame, scans a 17-wide (dx -8..8 step 2) × 19-tall (dy -9..9 step 3) window centered at (x, y-8) for any CRYSTAL(=26) cell, caching the result in lazily-added player._nearCrystal. While near a crystal: manaRegen = max(manaRegen, 1.3) (≈2.9× base) and 6%/frame chance of a rising cyan mote ([120,230,255], glow 1.8, grav -0.01). MATERIAL_PARAMS[CRYSTAL] = {resonance:0.80, bloomWeight:0.45} (L495).
**REC:** Port as-is (with CRYSTAL remapped to a new cell ID). Fits our region-graph world: crystals double as light sources and mana refuges. Keep the every-6-frames cache and the _nearCrystal flag as an explicit typed field, not a lazy add.

### digRay rune-strike hook [new] (L2361-2376)
digRay is identical to original except every 6th ray step (s%6===0) calls strikeRunes(gx,gy,7) BEFORE the hit test — so the excavate beam trips rune switches anywhere along its path, through air. strikeRunes (L5303-5317, outside range) checks all runeVaults' switch positions within radius 7, sets v.active/v.opening, toast 'ANCIENT RUNE STRUCK', two-tone chime, shake +0.012, green burst; the vault's stone door then dissolves 3 cells/frame bottom-up (updateRuneVaults L5319).
**REC:** Port the hook when the rune-vault system (another agent's range) lands. In our architecture this is an event: spells/dig ray emits a 'runeStruck' probe through Ctx rather than calling a global. Note our secrets are breachable-material walls per DESIGN — rune vaults are an additive second secret type, grid-explained (the door is real STONE cells), so it passes the 'grid explains it' commandment.

### firePlayerSpell: 15-spell dispatch with 8 new payloads [overlaps-ours] (L2423-2520)
Dispatch grows from 7 to 15 spells; every projectile now carries mul (dmgMul). Legacy 7 (bolt/bomb/lightning/flame/dig/warp/blackhole) are byte-identical to original incl. all SPELL_PARAMS numbers (verified vs original L358-367); bolt life 180, warp life 90, blackhole life 240 unchanged; lightning now passes mul to castLightning. NEW PAYLOADS (params at L504-520): (1) scatter 'Scatter Shot' — 5 pellets, type 'pellet', vel 9.5×(0.85+rnd*0.3), angle ±0.21 rad (spread 0.42), life 70, explosionRadius 6, mana 15, cd 20, playZap+playBoom(4). (2) embers 'Ember Storm' — 16 EMBER flying particles, angle ±0.275, speed 2.6-4.8 with vy bias -0.8, particle life 200-320, glow 2.0, grav 0.05, mana 18, cd 30. (3) icelance 'Ice Lance' — projectile vel 16, life 90, damage 30, freezeFrames 150 (consumed by projectile-hit code; enemies have chill/frozen fields), mana 22, cd 35, 1400Hz sine. (4) acid 'Vitriol Spray' — continuous (cd 0, mana 1.7/frame), 3 ACID particles/frame, angle ±0.15 (spread 0.30), speed 3.0-5.0 + 0.4×player.vx inherit, particle life 30-45, grav 0.06, glow 1.4. (5) ice 'Frost Shard' — projectile type 'iceshard', vel 11, life 140, damage 16, freezeRadius 7, mana 16, cd 16. (6) wisp 'Seeking Wisp' — projectile type 'wisp', vel 4.5, life 260, damage 13, mana 8, cd 8 (homing behavior lives in projectile update, outside range). (7) wall 'Conjure Stone' — calls conjureStone at mouseGridPosition, mana 18, cd 24. (8) meteor 'Meteor Call' — projectile vel 6.5 with vy-1.0 lob, life 300, explosionRadius 40, mana 70, cd 140, playBoom(8). Ownership is NOT checked here — hotbar UI gates it.
**REC:** Do not port the fixed dispatch — our wand/card system (src/combat/wands/*) replaced it by design. Port the 8 new payloads as new projectile CARD_DEFS + projectile types in Projectiles.ts, adopting their numbers verbatim. scatter is partially redundant with our spread charm + multicast (a 5-pellet payload is still worth having as one card); acid spray parallels our flame jet pattern (4-frame burst card); wall/conjure-stone and meteor and wisp and the two ice spells are genuinely new card material. mul folds into the compiler's damage multiplier (see dmgMul entry). Their mana costs assume no-wand economics — rebalance against our card costs (our spark=10 vs their bolt=12).

### conjureStone (Conjure Stone / 'wall' spell implementation) [new] (L2522-2549)
Raises a radius-6 disc of STONE at the cursor, range-clamped to 130 cells from the wand origin (x, y-9) — beyond that the target point is pulled back along the ray. Only writes into EMPTY cells. Anti-entombment: skips any cell overlapping the player (|X-px|<=5 and py-18<=Y<=py+1) or any enemy (|X-ex|<=halfW+1 and ey-h-1<=Y<=ey+1). Finishes with 8-smoke burst + playDig. Comment: 'Doubles as a puzzle tool — conjured stone weighs down pressure plates' (mechanisms system has plates at L5290).
**REC:** Port as a card ('Conjure Stone'). It is the DESIGN 'stone bridges you cast' verb made real and feeds the sensor framework (pressure plates read raw cells, so conjured stone triggering plates is automatic in our architecture). Keep the anti-entombment rule exactly — it prevents kill-by-cast griefs; in our port use ENEMY_DEFS dims from Ctx.

### updatePlayer ordering shuffle + minor flow changes [cosmetic] (L2174-2313)
Slime-absorb block (heal 0.9/cell, max 3/frame, identical numbers) moved BEFORE the movement integration (original ran it after aim, near the end). Aim/facing-from-aim, firing dispatch, animation call unchanged. New per-frame decrements at top: tpCool, fxTimers.speed/berserk, all buffs. Hazard block can early-return via killPlayer same as original. updatePlayerAnimation (2315-2352), wandTip (2354-2359), erodeAt (2377-2396), executeWarp (2398-2421), cellBlocks/entityFree/crushLooseDebris/tryMoveEntity (2019-2095) are all byte-identical to the original — zero changes.
**REC:** Skip the reorder (no observable effect worth chasing). Confirms our ports of physics/animation/warp/erode need no updates from this range. Our animation additionally has landing dust/shake/thud feedback — keep ours.

### Perks system (Sanctum boons) — player-side integration points [new] (L2097-2120, 2183, 2198-2201, 2240, 2253)
10 one-shot boons chosen between depths (PERKS L6053-6064, 3 random un-owned offered per Sanctum, picking one unlocks descent): vitality (+30 maxHp, full heal, applied directly — not a flag), manafont (mana regen ×1.6), feather (levit drain ×0.55), blast (explosion dmg ×0.4), pyro (fire/lava dmg ×0.4 — both in damagePlayer src path AND inline hazard DPS), toxicology (acid/toxic ×0.25, both paths), vampirism (+2 hp per kill — consumed in killEnemy, outside range), goldsense (key always on minimap, outside range), swift (move ×1.18), power (all spell damage ×1.25 via dmgMul). Stored as boolean flags in player.perks.
**REC:** Port the perk effects into our player/status/wand systems (the integration points in this range are small multipliers). The Sanctum UI/shop itself is another agent's range. Note partial overlap with our card economy and brewing as progression sources — perks are a third axis; recommend porting as-is for run-scoped meta-choices since DESIGN has no equivalent (closest is the Refuge shop).

**NOTES:** RANGE SHAPE: 2000-2551 is exactly the '===== Player (the Alchemist) =====' block: player const through conjureStone (2549); 2551 is the '===== Enemies =====' header. Immediately past the boundary (2552-2559) ENEMY_DEFS adds 3 new enemies (bat hp16, spitter hp55 w/ TOXIC gore, bomber hp34 death-explodes r24) and spawnEnemy adds depth scaling (hp ×(1+0.16(depth-1)), dmg ×(1+0.10(depth-1))) plus chill/frozen fields — belongs to the enemy-range agent but confirms icelance/iceshard freeze targets exist. HAZARDS FOR THE PORT FLEET: (1) Cell-ID ABI collision is the big one — new file's TOXIC=21/HEALIUM=22/TELEPORTIUM=23 vs our ElixirLife/Levity/Stone at 21/22/23; new file also claims 24-30 (SNOW/COAL/CRYSTAL/FUNGUS/GLASS/ASH/GLOWSHROOM). One canonical remap table (their 21-30 → our 24+) must exist BEFORE any agent ports cell-touching code, and CELL_COUNT must grow. (2) Hazard DPS still bypasses damagePlayer and invuln (direct hp subtraction), so perk reductions are duplicated inline in the sample loop — if you port resistances only into damagePlayer you silently miss half the fire/acid damage paths; our sampleAndTickStatus is the right single home. (3) hazardSrc local (L2191-2201) is assigned but never read — dead code, do not port. (4) The new file has TWO parallel timed-effect stores (fxTimers from flask brews, buffs from potion pickups) with overlapping semantics (fxTimers.speed 1.45× vs buffs.swift 1.5×, and they STACK multiplicatively with perks.swift to ×2.57 max run speed — possibly unintended); consolidate into our single status struct on port. (5) Legacy 7 SPELL_PARAMS are byte-identical to the original — zero retuning, so all observed combat changes come from the 8 new spells, mul, and perks. (6) Physics block (cellBlocks/entityFree/crushLooseDebris/tryMoveEntity), updatePlayerAnimation, wandTip, erodeAt, executeWarp, findSpawnPoint are byte-identical to the original — our existing ports stand; the new file completely LACKS our movement-feel work (coyote/jump-buffer/levit ramp/air control/hitstop/landing feedback), so keep ours as the base everywhere. (7) firePlayerSpell never checks player.spells ownership — gating is hotbar-UI-side; in our port the wand bench naturally replaces this. (8) mouseup bomb throw (L6534, outside range) does carry mul: dmgMul() — relevant to the projectile-range agent. (9) strikeRunes is also called from explosion/projectile paths elsewhere; the dig-ray hook here samples only every 6th step with radius 7, so a thin wall can't hide a rune from the beam.

## enemies-director
### Three new enemy kinds in ENEMY_DEFS (bat, spitter, bomber) [new] (L2552-2559)
ENEMY_DEFS grows from 3 to 6 kinds. Original slime/imp/golem stats unchanged (48/40/170 hp etc., verified byte-identical vs original 1883-1887). New: bat {hp 16, halfW 3, h 5, bounty 15, gore BLOOD}; spitter {hp 55, halfW 5, h 11, bounty 60, gore TOXIC, goreFn getToxicColor}; bomber {hp 34, halfW 5, h 8, bounty 45, gore FIRE}.
**REC:** Port as-is alongside our roster. Our 3 extras (acidslime/wisp/mage in src/entities/Enemies.ts) are a DIFFERENT trio — no name collisions; combined roster would be 9 kinds. Spitter's TOXIC gore requires the TOXIC cell to exist first (must be remapped, see notes).

### Bat AI — erratic flying melee swarmer [new] (L2730-2750)
No gravity; bobPhase += 0.22 per frame with vy += sin(bobPhase)*0.08 wobble. Within pDist<320 accelerates toward player at 0.14/frame on the normalized vector (x and y), else random walk ±0.05. Speed clamp ±1.7 (scaled by chill factor). Escapes solids by y-=1, vy=-0.6. Bite: attackCd 0 and |pdx|<8, |pdy|<12 → damagePlayer(6*dmgK, knockback -2.2/-1.6, 'impact'), attackCd=50, then darts away (vx=-sign(pdx)*1.6, vy=-1.0). Integrates via imp-style sub-cell drift (NO grid collision, clamped x[6,W-7] y[14,H-7]).
**REC:** Port as-is. Distinct niche from our wisp (frost ranged standoff flyer) — bat is the cheap melee pressure unit the roster lacked. In our port it joins the imp/wisp drift branch of the movement integrator.

### Spitter AI — rooted acid-lobbing turret + 'acidglob' hostile projectile [new] (L2751-2764 (death splash 2636-2639; projectile handling outside range at 1748, 1816-1818, 1890-1892))
Grounded (gravity 0.33), vx *= 0.4 every frame so it barely moves. When attackCd 0 and pDist<280: lobs projectile type 'acidglob' from (e.x, e.y-def.h) with arc aim atan2(pdy - min(60, pDist*0.35), pdx), speed 2.6 + pDist*0.006, extra vy -1.4; attackCd = 150-199; sets e.recoil=14 (sprite telegraph, LAZILY CREATED field — not in spawnEnemy initializer). Glob physics (other range): gravity 0.12/frame; on player hit (dSq<85) damagePlayer(8, kx p.vx*1.3, -1.6, src 'acid') + splashLiquid(ACID, r3); on terrain: splashLiquid spawns REAL acid cells radius 3. On death (killEnemy): no blood — 40-particle TOXIC burst + splashLiquid(TOXIC, r4) dumps real toxic sludge into the grid. damageEnemy also excludes spitter from blood spray (fire-colored sparks instead, line 2597-2607).
**REC:** Port as-is but reconcile with our acidslime: both own the 'acid enemy' niche (ours = melee slime sweating an acid trail, theirs = ranged artillery). Keep both; they read differently. Give spitter acid immunity in enemyEnvironmentDamage like we gave acidslime (the new file forgot — it takes 0.9/sample from its own splash). Declare recoil:0 in the typed Enemy initializer.

### Bomber AI — suicide hopper with fuse, explosive death [new] (L2765-2787 (death branch 2618-2632))
Fast slime variant: gravity 0.30, hops toward player every 32 ticks within pDist<300 (vx sign(pdx)*(2.4-3.2), vy -2.8..-3.6), wanders every 110 ticks. At pDist<34 lights fuse: e.fusing=36 (LAZILY CREATED field), 900Hz square tone; while fusing vx*=0.5; at fusing==0 calls killEnemy on itself. killEnemy has a dedicated bomber branch: triggerExplosion(e.x, e.y-4, radius 24-26) — full terrain destruction, damages player (cap 42 within r*1.5) AND other enemies (r*1.6, so bombers chain), then coins (bounty/10 = 4, homing in play), 12% potion drop, vampirism heal, waveState.kills++, NO gibs/blood/squelch/shake. CRITICAL: the bomber branch runs on ANY death, including being shot — killing one point-blank still detonates it.
**REC:** Port as-is — the always-explodes-on-death rule is the interesting mechanic, keep it. Declare fusing:0 in the typed initializer. In our Ctx port, killEnemy→triggerExplosion→damageEnemy reentrancy during the backwards updateEnemies loop is safe (splice at <= i), but our explosion module must tolerate being called from inside Enemies.update.

### Depth-scaled enemy stats (hpMul / dmgK) in spawnEnemy [new] (L2574-2584)
Play mode only: hpMul = 1 + (depth-1)*0.16 applied to hp/maxHp (rounded); dmgK = 1 + (depth-1)*0.10 stored per-enemy. dmgK is ONLY applied at two call sites: slime melee (12*dmgK, line 2727) and bat bite (6*dmgK, 2746). Golem melee (20), golem rocks (hostileDmg 9), imp fireballs (11), and spitter globs (8) are all flat — inconsistent, probably an oversight. spawnEnemy now also returns the pushed enemy object (original returned undefined). New per-enemy fields: dmgK, chill, frozen.
**REC:** Adopt the numbers — our port scales population COUNT with depth (worldgraph.populationForDepth) but not per-enemy stats; this is complementary, not conflicting. When porting, either apply dmgK uniformly to all six damage sources or document the exemption; flat golem/imp damage at depth 5 is a real balance difference (golem 20 vs slime 16.8).

### Chill / frozen enemy status timers (frost-spell debuffs) [overlaps-ours] (L2580, 2693-2709 (plus golem gating 2837, 2861))
Two countdown timers on every enemy. frozen (set to SPELL_PARAMS.icelance.freezeFrames by the icelance projectile, line 1794): while >0 → vx *= 0.78/frame, attackCd floored at 30 (cannot attack), jetFuel zeroed, 4%/frame frost-mote particles; golem also refuses to ignite jets or thruster-vault while frozen. chill (set to 140 by iceshard hits, line 1840): movement factor cf = 0.45 multiplies every AI impulse and speed clamp (slime hops, bat/imp accel+clamps, bomber hops, golem clamp). Decrement 1/frame each.
**REC:** Merge with our sim-sampled status system (src/entities/status.ts: sampleAndTickStatus every 2nd frame reads cells touching the body, returns {damage, slowFactor}). Theirs is spell-event-driven timers; ours is grid-driven. DESIGN pillar 5 mandates one shared status struct — map their frozen→status.frozen frames and chill→status slowFactor 0.45 as spell-written entries in OUR struct rather than adding parallel ad-hoc fields. Keep their concrete numbers (0.78 decay, cf 0.45, chill 140, attackCd floor 30, jet lockout) as the tuning.

### Off-screen enemy AI freeze gate [new] (L2691-2692)
At the top of the per-enemy loop: if e.x/e.y is more than 60 cells outside the sim window (sim.x0-60 .. sim.x1+60, same for y) the enemy is skipped entirely — no AI, no gravity, no environment damage, no timers. Enemies 'wake when you approach'. Original updated every enemy every frame regardless of distance.
**REC:** Adopt — essential once levels carry 36+ placed enemies (ours carry up to ~67). Matches our existing electrical-grid precedent (charges freeze outside the window). Note interaction: frozen/chill/fusing timers also pause off-screen, and a lit bomber fuse suspends if you outrun the window.

### killEnemy: potion drops + vampirism lifesteal + per-kind gore [new] (L2618-2667)
On any kill in play mode: if player.perks.vampirism, heal +2 hp (capped at maxHp). Potion drop roll: 30% for golem, 12% everything else → addPickup(e.x, e.y-5, 'potion', random of POTION_KINDS, pop=true). Gore retuned per kind: bat gets reduced bursts (24/10 blood vs 46/24, 7 gouts vs 16, stain r5 vs r10); spitter replaces blood entirely with toxic shower (see spitter entry); golem unchanged (62/24, stain r14). Coins/score/build-mode bounty logic otherwise identical to original.
**REC:** Port the drop table and vampirism hook once the pickup (addPickup/POTION_DEFS, ~line 5361) and perk systems land from their ranges; the hooks themselves are two guarded lines, cheap to carry. Gore tuning is cosmetic — adopt numbers.

### Level Director replacing the Wave Director [overlaps-ours] (L2931-2997)
startWave/updateWaves are DELETED (grep confirms no callers). waveState survives only as {kills} counter (line 2933, reset at 6659); showBanner kept as a generic UI helper. Three new functions: (1) pickEnemyKind (2944-2955): weighted roll over BIOMES[levelState.biome].foes — 8 biomes each declare e.g. earthen [[slime,5],[bat,3],[imp,2],[golem,1]], fungal [[slime,4],[spitter,4],[bat,3],[bomber,1]], frozen/flooded/timber/crystal/scorched/volcanic similar (lines 4544-4619); fallback [[slime,5],[imp,3],[golem,1]]. (2) populateLevelEnemies (2958-2981): one-shot seeding on level entry — count = min(36, 12 + depth*3), up to 400 placement tries; candidate must be an EMPTY cell, >=260 cells from the player (landing-zone calm), >=120 cells from portalObj (anti-camp); then forced guardSpawns (golem+imp+bat trio pushed at the golden-key vault, lines 5818-5820) and the array is cleared. Gated by levelPopulated flag, invoked from closeSanctumAndDescend (6153) and level entry (6602). (3) updateDirector (2984-2997): trickle respawner in the main loop (called at 4310, after updateEnemies) — every 720 frames (~12s), if enemies.length < cap (10 + depth*2), spawns 1 (60%) or 2 (40%) enemies just off-screen at x = player.x ± (VIEW_W*0.62 + 0..120), y = player.y - 60..+60; suppressed while dead/paused/unpopulated.
**REC:** Merge with our Levels.placePopulation (src/game/Levels.ts:218, clearance 220) — ours already does one-shot placed populations via populationForDepth (per-kind counts up to ~67/level, region-graph-aware), which is richer than their flat min(36, 12+3d) + biome weights; keep OUR placement but ADOPT their portal/exit anti-camp exclusion (120 cells) and the guardSpawns forced-spawn hook for key vaults. The trickle updateDirector CONTRADICTS docs/DESIGN.md pillar 9 ('placed populations, finite, readable; nest-clear rewards') — recommend SKIP, or gate it behind a biome/danger flag with telemetry before committing. Also: our WaveDirector.ts is now dead in play mode (constructed at Game.ts:116 but only ctx.levels.update runs) — the new file confirms the same conclusion; safe to retire it to build-mode-only or delete.

### Core sim dispatcher: 10 new material entries [new] (L2999-3064)
processSimulationFrame keeps the exact original order (harvester → electrical → projectiles → shockwave aging → movedGrid clear over sim window → bottom-up randomized-direction sweep → second pass). Changes: (a) static skip list adds CRYSTAL(26), GLASS(28), FUNGUS(27), GLOWSHROOM(30) alongside EMPTY/WALL/WOOD/STONE/METAL/ICE/VINES — four new inert-in-main-pass solids. (b) New dispatch entries: SNOW(24)→handleSnow (light powder, 0.7 fall chance, floats on water, melts to WATER near FIRE/LAVA/EMBER at MATERIAL_PARAMS[SNOW].meltChance); COAL(25)→handleCoal (powder; ignites to FIRE with life burnDuration+0..39 near FIRE/LAVA at igniteChance — long-burn fuel); ASH(29)→handleAsh (featherlight drift powder, 35% sideways drift, 55%/50% fall rolls, 10% dissolve adjacent to WATER); TOXIC(21)/HEALIUM(22)/TELEPORTIUM(23)→handleExoticLiquid (viscous flow via handleViscousLiquid, then: HEALIUM evaporates to pink STEAM at evaporationSpeed; TELEPORTIUM 0.4% violet glow motes; TOXIC 0.15% smoke). (c) Second (in-place CA) pass adds FUNGUS→handleFungus next to ICE/VINES: vine-like energy-budget creeper (lifeGrid 0=fresh→16-37 energy, -1=mature/glowing; spreads at spreadRate into EMPTY cells that touch a solid, child energy = parent-2-4, 6 directions with up-bias). (d) handleFire (just above range) now leaves ASH 10% of the time when burning out over a non-empty cell. BLOOD inline stain/soak-in numbers are unchanged from the original. updateSimulation accumulator (6-substep cap) is byte-identical.
**REC:** Port the dispatcher entries into src/sim/Simulation.ts processFrame with the shared remap already established in the current TS port: Toxic=24, Healium=25, Teleportium=26, Snow=27, Coal=28, Crystal=29, Fungus=30, Glass=31, Ash=32, Glowshroom=33; Moss occupies 34 and `CELL_COUNT` is 35. HEALIUM remains distinct from brewed ElixirLife for now. FUNGUS/GLOWSHROOM/CRYSTAL/GLASS also need isSolid/blocksEntity predicate updates (their definitions live outside this range).

### damagePlayer gains a damage-source tag (4th arg) [overlaps-ours] (L2727, 2746, 2907 (definition at 2104-2120, outside range))
All enemy melee calls now pass 'impact' as a 4th src argument; acidglob passes 'acid', hostile fireballs 'explosion'. The definition (2104) uses src for perk resistances: blast perk ×0.4 on 'explosion', pyro ×0.4 on 'fire', toxicology ×0.25 on 'toxic'/'acid'; stoneskin buff ×0.4; floor of 0.5 damage. killPlayer game-over now reports 'DEPTH n' from levelState instead of wave number.
**REC:** Adopt the signature change in our PlayerControlApi.damage (src/entities/Player.ts currently 3-arg). The source tag is also exactly what our telemetry mandate (deaths-by-cause counters, DESIGN 'What the panel missed') needs — port it as a typed union ('impact'|'fire'|'acid'|'toxic'|'explosion'|...) even before perks land.

### slime/imp/golem AI: zero retuning [tuning] (L2713-2910)
Diffed line-by-line against original 1996-2135: every constant identical (slime hop 260/50/130 melee 12@45cd; imp standoff 130, y-75, fireball 3.6 cd 130-200; golem accel 0.12 clamp 0.78, jets 0.58 thrust / fuel 95-144 / cd 280 / triggers 28-above-within-230 or vy>2.3, vault 3-12, pound stuckT>50→26 erodeAt r6, rocks 3×9dmg 50-360 cd240, melee 20 cd 220, powder smash, movement integration with golem stepUp 2). Only deltas are the cf/dmgK/frozen injections covered above, and the integration branch admitting bat to the drift path and spitter/bomber to the collide path.
**REC:** No action — our ported Enemies.ts already matches these numbers exactly. Only the cf/dmgK/frozen hooks need adding.

**NOTES:** CELL-ID ABI HAZARD (the big one): the new file's TOXIC=21/HEALIUM=22/TELEPORTIUM=23 (declared at its lines 465-466) collide with our save-format-stable ElixirLife/ElixirLevity/ElixirStone 21/22/23 — every dispatcher entry, ENEMY_DEFS gore field (spitter uses TOXIC), splashLiquid call, and BIOMES poolElement in this range must be remapped to appended IDs 24+ on port; HEALIUM additionally duplicates our ElixirLife's role and should probably be unified rather than coexist. LAZY FIELD HAZARD: spawnEnemy's initializer (2577-2584) omits recoil (spitter) and fusing (bomber); both are created on first use — a typed TS Enemy must declare them (=0) or the spitter telegraph/bomber fuse logic silently works off undefined. REENTRANCY: bomber death calls triggerExplosion from inside killEnemy from inside the updateEnemies backwards loop; explosions damage other enemies (r*1.6) so bombers chain-detonate and can splice multiple enemies mid-iteration — safe under backwards iteration + the existing identity recheck, but our Ctx explosion module must be callable from Enemies.update. DESIGN CONFLICT: updateDirector's 12-second trickle respawner ('never feels fully cleared') directly contradicts docs/DESIGN.md pillar 9's finite, readable placed populations and the nest-clear reward loop — flag for a design decision rather than porting silently. INCONSISTENCY worth fixing on port: depth dmgK multiplies only slime melee and bat bite; golem melee/rocks, imp fireballs, and spitter globs stay flat at all depths. Also: spitter lacks the acid self-immunity we gave acidslime (takes 0.9/sample from its own splash residue). Off-screen freeze gate (±60 cells beyond sim window) pauses ALL enemy timers including lit bomber fuses. Our WaveDirector.ts is dead code in play mode (constructed, never updated) — the new file reached the same end state by deleting startWave/updateWaves and keeping waveState only as a kill counter; safe to retire ours. Sim frame order contract is fully preserved (harvester → electrical → projectiles → shockwaves → moved-clear → sweep → in-place CA pass), so the port is purely additive dispatcher entries plus a FUNGUS line in the second pass. Files: new = Y:/source/VibeCodingGames/Noita-sandbox/noita-alchemists-descent.html (range 2551-3076), original = Y:/source/VibeCodingGames/Noita-sandbox/noita-sandbox.html 1882-2275, ours = Y:/source/VibeCodingGames/Noita-sandbox/src/entities/Enemies.ts, src/game/WaveDirector.ts, src/game/Levels.ts, src/config/worldgraph.ts, src/sim/Simulation.ts, src/sim/CellType.ts, src/entities/status.ts.

## render-lighting
### setPx/addPx pixel writers [cosmetic] (L3077-3088)
Byte-identical to original L2278-2289 (world->view transform via renderCamX/Y, Y-flipped index, setPx writes alpha 1.0, addPx accumulates without touching alpha).
**REC:** skip because ours is better — already ported into src/render/FrameComposer.ts with the same semantics.

### Player wizard sprite [cosmetic] (L3090-3228)
Identical to original L2291-2430 except draw-order swap: wand tip pulse + muzzle flash (tip = wandTip(), pulse 0.8+sin(frameCount*0.3)*0.2, flash on frameCount%4<2 while firing) now render BEFORE the charged-throw meter, so the 8-dot bomb meter overdraws the flash. Original drew meter first. Also drops redundant local `const ca = player.aimAngle`.
**REC:** skip — keep our src/render/sprites/PlayerSprite.ts; optionally mirror the z-order swap (1-line move) so the meter stays readable while firing.

### Frozen/chill tint on enemy body lighting [overlaps-ours] (L3237-3241)
Body light multipliers changed from const to let, then modulated by status: e.frozen>0 -> bR*=0.55, bG*=0.9, bB=min(1.8, bB*1.5+0.18); e.chill>0 -> bR*=0.55, bG*=0.85, bB=min(1.8, bB*1.3+0.25). Applies to ALL non-flash body pixels (P), not emissive (PE), so a frozen enemy reads as a blue-shifted silhouette even in darkness (the +0.18/+0.25 additive floor guarantees visibility). Imps (normally fully self-lit, multipliers 1) also get tinted when frozen: bB becomes 1.68.
**REC:** merge with our sim-sampled status system — src/render/sprites/EnemySprites.ts computes the same bR/bG/bB but has no status tint. Adopt their exact numbers, driven by our FROZEN/chill status fields instead of raw e.frozen/e.chill.

### Bat enemy sprite [new] (L3389-3412)
New 'bat' kind, ~9px wingspan. Hover bob = round(sin(e.bobPhase)*1.2). 2-pose wing snap on frameCount%10<5: raised pose puts wing pixels at dy 3-4 spanning x +-2..+-4; swept pose at dy 0-1. Body: 3x2 purple nub (V=[0.36,0.22,0.46], VD=[0.20,0.11,0.27]) + 2 ear pixels at dy 3. Emissive red eyes via PE: intensity (0.8+rand*0.5)*boost*0.4 red with 0.04 G/B, eye x depends on look direction (look==1 ? 0,1 : -1,0). Deliberately NO seedLight entry in buildLighting — bats are invisible in the dark except their glinting eyes (a dark-hunter tell).
**REC:** port as-is into EnemySprites.ts as a new case (AI/spawn live in other ranges); preserve the no-light-emitter choice — it is the enemy's design identity.

### Spitter enemy sprite + charge-up light tell [new] (L3413-3440, 3673-3676)
New rooted plant-turret 'spitter'. Root claws: dy0 pixels at x -4..4 step 2, dy1 full row x -3..3 (dark green TD=[0.16,0.32,0.10]). Stalk dy2-5 sways: offset = round(sway*(dy-1)/5), sway = sin(frameCount*0.04 + e.bobPhase)*1.2. Bulb head centered at hy = 8 - recoil, recoil = round(e.recoil*0.18) (e.recoil set by its lob attack elsewhere; head visibly dips after firing). Head is 5 rows, half-width 3 (2 at top/bottom), body T=[0.30,0.55,0.18]. Emissive maw: 2 px facing player at (sway+look, hy) and (sway+look*2, hy), brightness mawG=(0.5+charge*0.9)*boost*0.5 where charge=(1-e.attackCd/40) when attackCd<40 — maw brightens over the final 40 frames before each shot. Venom-sac highlight px (TB=[0.55,0.85,0.25]) at (sway-2,hy+1),(sway+2,hy-1). Matching buildLighting seed: when attackCd<40, ch=(1-attackCd/40)*0.5, seedLight(e.x, e.y-8, ch*0.5, ch, ch*0.15) — the charge-up is readable in total darkness.
**REC:** port as-is (sprite case + the lighting-loop seed). The wind-up light tell is load-bearing combat telegraphy in dark caves — keep sprite charge formula and light seed using the same attackCd<40 window so they stay in sync.

### Bomber enemy sprite + accelerating fuse strobe [new] (L3441-3472, 3670-3672)
New 'bomber' kind: orange slime reusing slime squash/stretch math exactly (air stretch sy=1+min(0.45,|vy|*0.13); landing splat 8 frames sx=1+splat*0.05; idle wobble freq 0.12 amp 0.09 — faster/jigglier than slime's 0.085/0.07). Mutates e.splat/e.prevG at render time like slime. Fuse behavior when e.fusing>0: strobe period = max(1, floor(e.fusing/6)) frames, i.e. flashing ACCELERATES as remaining fuse decreases; strobing frames swap body palette to near-white (O=[2.0,2.0,1.6], OD=[1.4,1.4,1.1]) AND route body pixels through PE so the strobing bomber is fully self-lit ('a strobing bomber lights itself'). 2px stub fuse at (0,H),(0,H+1); while fusing an emissive spark at (0,H+2): (1.4+rand*0.8)*boost*0.5 warm. Idle: 2 black bead eyes at eyeY=round(H*0.4). Matching buildLighting seed while fusing: strobe=(floor(frameCount/3)%2===0)?1.6:0.5; seedLight(e.x, e.y-4, strobe, strobe*0.85, strobe*0.5) — fixed 3-frame light strobe (note: light strobe cadence differs from sprite strobe cadence, and lighting only rebuilds on even frames, so they intentionally desync).
**REC:** port as-is, keeping the period=floor(fusing/6) formula — the accelerating flash is the entire dodge-window telegraph. Acceptable to unify light strobe with sprite strobe, but test in dark scenes first.

### Golem jet light seed removed [tuning] (L3663-3669)
Original L2729 seeded seedLight(e.x, e.y+2, 1.5, 0.9, 0.22) while e.jetFuel>0; the new file's enemy lighting loop drops it. The golem sprite still draws thruster flame pixels (3382-3388), so jets now show flames that cast no light.
**REC:** skip because ours is better — src/render/Lighting.ts:205 kept the jet seed and it is more physically consistent; looks like an accidental deletion during the enemy-loop rewrite.

### Translucent-material light attenuation tier (ICE/GLASS/CRYSTAL) [tuning] (L3548-3550)
lightAtt gains a fourth tier: EMPTY/gas 0.86 > CRYSTAL/GLASS/ICE 0.84 > liquid 0.80 > solid 0.40. Original classified ICE as solid (0.40). Light now propagates through ice walls, glass panes, and crystal nearly as well as through water — frozen-biome caves glow through their walls, and the raycastLight transmittance bucket changes too (att 0.84 falls in the 'liquid' 0.93/step bucket instead of 'solid' 0.60/step, via the unchanged thresholds 0.5/0.88).
**REC:** adopt their numbers — apply 0.84 to our Cell.Ice immediately in src/render/Lighting.ts:110 (D3 Frozen depends on this readability), and include glass/crystal when those cells are ported (with remapped IDs).

### Six new emissive material light seeds [new] (L3563-3580)
buildLighting's per-cell emissive chain adds: FUNGUS(27): f=0.30+sin(frameCount*0.04+wx*0.3+wy*0.2)*0.08, spatially-phased teal pulse, sets G=f (floor), R>=f*0.25, B>=f*0.8. CRYSTAL(26): cf2=0.42+sin(frameCount*0.05+wx*0.7)*0.10 cyan shimmer, B=cf2 floor, R>=cf2*0.35, G>=cf2*0.85. GLOWSHROOM(30): static G=0.40 floor, R>=0.16, B>=0.20. HEALIUM(22): pink, R=0.3 floor, G>=0.12, B>=0.2. TOXIC(21): dim sickly green, G=0.18 floor, R>=0.05, B>=0.03. TELEPORTIUM(23): violet random flicker f=0.28+rand*0.08, B=f floor, R>=f*0.6, G>=f*0.2. All use the same max-merge floor pattern as the original ACID/GOLD seeds. chargeGrid branch unchanged but now last in an 11-deep else-if chain.
**REC:** port with mandatory ID remap (their 21/22/23 collide with our ElixirLife/Levity/Stone). If HEALIUM maps to ElixirLife, reuse its pink glow there. Per DESIGN's data-driven philosophy, convert the else-if chain to an emissive lookup table keyed by cell type — this hot loop runs ~47k half-res cells every other frame.

### Six new projectile light seeds [overlaps-ours] (L3599-3604)
Projectile glow table gains: pellet (0.3,0.7,0.9), iceshard (0.5,0.9,1.3), icelance (0.45,1.0,1.5), wisp (0.4,1.1,1.2), meteor (1.9,0.9,0.2 — brightest projectile in the game, exceeds fireball's 1.5), acidglob (0.15,0.7,0.1). Original five (bolt/fireball/bomb/warp/blackhole) unchanged.
**REC:** merge — our wand/spell-card system (src/combat/wands) defines its own projectile types; map these seeds onto whichever card projectiles we adopt. Also fix our preexisting gap: FrameComposer renders 'frostbolt' but Lighting.ts never seeds it — borrow iceshard's (0.5,0.9,1.3).

### Portal light (level exit) [overlaps-ours] (L3606-3614)
portalObj {x,y,open,age}: when open, pf=1.1+sin(age*0.07)*0.35 and seedLight(x, y-10, pf*0.7, pf*0.4, pf*1.6) — a pulsing violet flood that marks the exit chamber from far away. When locked, steady (0.35,0.28,0.12) gold glint. Portal sprite itself (drawPortal, line 5533) is outside this range.
**REC:** merge with our X — our descent uses breakable wells (src/game/Levels.ts) not portals; adapt the open-portal violet pulse as the lit-well/next-depth beacon, and the locked gold glint for sealed wells. Numbers transfer directly.

### Brazier light (lit landmark mechanism) [overlaps-ours] (L3616-3621)
mechanisms[] entries with kind=='brazier' and lit==true seed a warm fire flicker: bf=0.9+rand*0.4, seedLight(m.x, m.y-2, bf*1.2, bf*0.7, bf*0.18). Slightly stronger than a raw FIRE cell (R up to 1.56 vs 1.4) so braziers read as landmarks over ambient flame.
**REC:** adopt their numbers for our fire-lit waystones (DESIGN pillar 10 — 'checkpoints you earn by bringing fire'). This is exactly the lit-waystone glow we need; wire it to our waystone lit state.

### Pickup glow table [new] (L3622-3629)
Per-pickup seedLight: key (0.7,0.55,0.12) @y-4, tome (0.45,0.3,0.8) @y-4, heart (0.55,0.12,0.2) @y-4, chest (0.40,0.32,0.08) @y-2, potion uses POTION_DEFS[p.data||'health'].light @y-2 (health [0.55,0.10,0.12], mana [0.12,0.25,0.60], stone [0.30,0.30,0.34], swift [0.50,0.40,0.08], levit [0.12,0.45,0.48], torch [0.55,0.35,0.10] — defs at L5353-5360), flask (0.18,0.22,0.3) @y-2. Items 'beckon' through darkness; brightness encodes value (key brightest, flask dimmest).
**REC:** port as a data table alongside the pickup system (system itself is another range's scope). Keep the value-encodes-brightness hierarchy when we add card/relic pickups.

### Rune-switch light states [new] (L3630-3635)
runeVaults[] (sealed strongrooms, L553): each vault's rune switch at (v.rx, v.ry) seeds pulsing violet while unsolved — rp=0.45+sin(frameCount*0.07+v.rx)*0.25, seed (rp*0.8, rp*0.25, rp); phase-offset by world x so multiple runes pulse out of sync. Once v.active: steady green (0.15, 0.7, 0.3). A binary lock-state indicator legible purely through light color.
**REC:** port with the runeVault system; the violet-pending/green-done color language should become our standard for sensor states (DESIGN pillar 8 sensor framework).

### Torch buff: brighter, steadier, longer wand raycast [new] (L3739-3744)
Wand shadow-casting raycast now reads player.buffs.torch (set to 1500 frames by the torch potion, L5461): torch active -> base brightness s=7.0 (vs 4.6), flicker term becomes 0.92+wandFlicker*0.12 (range ~1.016-1.074, i.e. nearly steady, vs raw wandFlicker 0.80-1.28), and raycast radiusHalf=84 (vs 56) — +50% light reach (168 vs 112 world px radius at half-res). RAYS=540 and transmittance constants unchanged.
**REC:** port as-is as a light-source modifier hook in src/render/Lighting.ts build(); drive it from our potion/status system. Cheap, high-value exploration upgrade that matches our cauldron-brewing pillar.

### Six new projectile sprite renderers [overlaps-ours] (L3959-3997)
pellet: head px (0.5,0.85,1.0)*boost + one trail px at -2*norm(v) (0.1,0.35*boost,0.5). iceshard: 2px head (0.75,0.95,1.2)*boost and (0.5,0.8,1.05)*boost, trail at -2n/-4n, plus a sparkle px every 3rd frame at random +-1 offset (0.55,0.75,0.95). wisp: 4-px cross-ish teal blob pulsing with pulse=0.8+sin(p.age*0.35)*0.35 (head 0.4/1.0/1.1 *boost*pulse). meteor: full 3x3 block, core (1.3,0.6,0.05)*boost*fl vs ring (0.9,0.3,0.05)*boost*fl with fl=0.85+rand*0.5, plus trail px at -2*sign(vx),-2*sign(vy) — biggest projectile sprite in the game. flask: 1px body tinted from FLASK_TYPES[p.flaskType].tint (water [0.25,0.5,1.2], oil [0.35,0.28,0.18], acid [0.4,1.2,0.25], lava [1.4,0.35,0.08], nitrogen [0.8,1.2,1.4], toxic [0.35,0.7,0.2], healium [1.4,0.6,0.9], teleportium [0.9,0.4,1.4], swift [1.3,1.1,0.3], berserk [1.4,0.25,0.25] — table at L6266) + a 70%-tint px and a cork-glint px (0.45,0.38,0.26) that swap positions every 5 frames (spin = floor(p.age/5)%2, a 2-phase tumble). acidglob: 3px green blob fl=0.8+rand*0.4 (head 0.35,1.0*boost,0.15) + drip px below every 4th frame. Original bolt/bomb/warp/fireball/blackhole renderers unchanged.
**REC:** port the renderers as new cases in src/render/FrameComposer.ts as their spells/flask land in our card system. Reconcile ice visuals: our 'frostbolt' (2x2 + additive halo) vs their iceshard/icelance — pick one ice family. The flask tumble sprite is required for our Flask pillar's throw verb.

### Entity overlay draw-order additions [new] (L4018-4022)
updateWebGLBuffers now calls drawMechanisms() -> drawRunes() -> drawPortal() -> drawPickups() between the projectile pass and the enemy pass (implementations at L5272/5335/5533/5469, outside this range). Full new layer order: grid composite -> flying particles -> lightning arcs -> projectiles -> mechanisms -> runes -> portal -> pickups -> enemies -> dig beam -> player. Dig beam still decrements digBeam.life inside the renderer (sim-in-renderer quirk preserved; ours moved it to Game frame tail per approved deviation 7).
**REC:** port the ordering contract exactly — world-fixture layers under living entities, player always last. Slot into our composeFrame() at FrameComposer.ts:343 before the enemy loop.

### icelance has light but no sprite [new] (L3601 (light), 3931-4016 (sprite switch))
icelance projectiles (SPELL_PARAMS L517: velocityForce 16, damage 30, freezeFrames 150) get the brightest cold light seed (0.45,1.0,1.5) but there is NO icelance case in the projectile sprite switch, and grep shows no in-flight trail particle spawn — in flight it is a fast-moving disembodied glow; only its impact shatter (L1856-1869, other range) is visible.
**REC:** flag for the port fleet: either intentional 'invisible cold spear' or an oversight. Recommend adding a sprite (reuse iceshard head + longer trail) when porting; do not silently replicate the gap.

### Parallax background / empty-cell composite / ambient — unchanged [cosmetic] (L3837-3865, 3893-3905, 3498)
Despite the focus brief, there are NO parallax, weather, or ambient changes in this range: empty-cell composite is byte-identical to original (scroll 0.35x/0.62x, base 0.022+bgNear*0.085, far silhouette *0.40, depth gradient 0.86+0.14*(1-wy/HEIGHT), air-glow 0.10/0.085/0.07), FIRE/LAVA/EMBER flicker identical, AMBIENT still let 0.14 mutated only by the inspector slider (L6648) — no per-depth ambient. New glow materials get no per-pixel flicker; they rely solely on light seeds + MATERIAL_PARAMS bloomWeight.
**REC:** skip — nothing to port. Note for our Levels.ts: the new file did NOT do per-depth ambient/backdrop tinting in the composer; if we want biome mood per depth, that is our own work, not a port.

**NOTES:** CELL-ID ABI HAZARD (most important): the new file defines TOXIC=21, HEALIUM=22, TELEPORTIUM=23, SNOW=24, COAL=25, CRYSTAL=26, FUNGUS=27, GLASS=28, ASH=29, GLOWSHROOM=30 (L462-466). 21/22/23 collide with our append-only ElixirLife/ElixirLevity/ElixirStone in src/sim/CellType.ts; every lighting/render branch in this range references these constants, so all ports must pass through one remap table (their 24-30 must shift too — our next free ID is 24). Suggested semantic pairing: HEALIUM~ElixirLife (both healing liquids, reuse the pink glow). Other hazards: (1) drawEnemySprite still mutates animation state at render time, now including bomber splat/prevG — the original quirk extends to new enemies; skipping draws still changes animation. (2) Three desynced clocks around the bomber: sprite strobe period floor(e.fusing/6), light strobe fixed 3 frames, lighting rebuilt only on even frames — visually fine, but a naive 'unification' would change the telegraph. (3) The golem jet light seed was deleted in the new file while the jet flame sprite remains — almost certainly accidental; our port kept it (Lighting.ts:205). (4) Our preexisting inconsistency surfaced by this comparison: FrameComposer renders 'frostbolt' but Lighting.ts never seeds light for it. (5) The emissive-seed else-if chain in buildLighting is now 11 branches deep inside the ~47k-cell half-res hot loop run every other frame — convert to a lookup table per the DESIGN frame-budget ledger. (6) Spitter sprite/light both key off attackCd<40 — keep that window in one constant or the dark-cave telegraph desyncs. (7) sampleSpriteLight still returns via _ltR/_ltG/_ltB out-globals in the new file; ours already converted to a returned LightSample (approved deviation 5) — port new callers (flask/bat/etc.) onto our API. (8) Cross-range references read from this range: portalObj/mechanisms/pickups/runeVaults state (L546-553), POTION_DEFS.light (L5353), FLASK_TYPES.tint (L6266), player.buffs.torch (set L5461), draw functions at L5272/5335/5469/5533 — coordinate with the agents covering those ranges so light tables and entity structs land together. Structurally surprising: ICE moving from the solid attenuation tier (0.40) to a translucent tier (0.84) is a one-line change with a large gameplay-readability effect in frozen biomes, and the DESIGN doc's 'translucent glass rendering' cut apparently did NOT cut translucent glass *lighting* — the new file ships it cheaply via lightAtt, which is a clever budget-respecting reinterpretation worth adopting.

## hud-loop-ui
### Expanded pixel-art icon set (17 new 11x11 icons + spell/material 'wall' disambiguation) [new] (L4148-4201, 4212-4243)
The 27 original icons (sand..blackhole) are byte-identical to original 3081-3160. Appended: 7 material icons for new cells — stone(4148), snow(4151), coal(4154), ash(4157), toxic(4160), healium(4163), teleportium(4166), crystal(4169), glass(4172), fungus(4175), shroom(4181) — and 6 spell icons — scatter(4178), embers(4184), icelance(4187), wisp(4190), wall_spell(4193), meteor(4196). Line 4201 aliases PIXEL_ICONS.wall_material = PIXEL_ICONS.wall because the new 'wall' SPELL key collides with the wall MATERIAL icon id; makeIconCanvas grew a third param ctx_spell (4212-4213) that remaps name 'wall'→'wall_spell' when rendering spell contexts, and injectToolbarIcons (4234) passes mode==='spell' through. All icons use the same {p: palette, g: 11-row grid} format as ours.
**REC:** Port the new icon grids into src/ui/icons.ts as-is (same format). Skip the ctx_spell hack: in our port, card icons resolve via cardIconName(), so give Conjure Stone the 'wall_spell' grid under a non-colliding name (e.g. 'conjurestone'). The healium/toxic/teleportium icons can also serve as visual bases for our existing Elixir cells 21/22/23 if desired.

### ELEMENT_ICON map extended to IDs 12 and 21-30 [new] (L4203-4210)
Adds 12:'stone' (the original map at 3162-3167 had NO entry for stone — stone had no toolbar button) plus 21:'toxic', 22:'healium', 23:'teleportium', 24:'snow', 25:'coal', 26:'crystal', 27:'fungus', 28:'glass', 29:'ash', 30:'shroom'. This confirms the new file defines 10 new paintable materials with IDs 21-30.
**REC:** ABI HAZARD — adopt with remap. Our CellType.ts already owns 21=ElixirLife, 22=ElixirLevity, 23=ElixirStone (append-only save ABI). Their TOXIC/HEALIUM/TELEPORTIUM must become 24/25/26 in our enum, shifting their snow..shroom to 27-33 (port fleet: define the canonical remap table ONCE and use it for every system that touches these IDs). Note glass/ash/fungus(spores) were already on our DESIGN.md year-one material list — this is convergent, adopt their IDs-remapped versions; snow/coal/crystal/shroom/toxic/healium/teleportium are net-new to us.

### Learned-spell hotbar (player.spells roster replaces fixed SPELL_ORDER in play mode) [overlaps-ours] (L4245-4264, 4292-4298)
buildHotbar now renders gameMode==='play' ? player.spells : SPELL_ORDER (original always used SPELL_ORDER of 7). player.spells starts as START_SPELLS=['bolt','bomb','flame','dig'] (line 522/2008) and grows in-run via rune vaults (line 5427) and the sanctum shop (line 6121-6124) from ALL_SPELLS (15 spells, line 521). Key labels are (i+1)%10 so slot 10 shows '0'; build-mode toolbar hotbar shows all 15 with wrapping labels. Slot click now also calls updateHUD(). updateHUD's selected/unaffordable loop iterates the same mode-dependent list. SPELL_ORDER is now just an alias for ALL_SPELLS.
**REC:** Skip the hotbar model itself — our Hud.buildHotbar mirrors the active wand's card slots (non-selectable, program runs left-to-right) and that supersedes per-spell selection. BUT adopt the progression idea: their rune-vault/sanctum spell unlocks map 1:1 onto our cardGranted economy (nest chests/shop/anvil from Wave D) — use their unlock sites as card-drop sources when porting vaults/sanctum.

### HUD descent readout: depth/biome labels, golden-key indicator, objective line, brew-buff chips [overlaps-ours] (L4266-4299)
updateHUD adds (vs original 3221-3236, which it otherwise matches exactly): #depth-label = 'DEPTH '+levelState.depth; #biome-label = BIOMES[levelState.biome].name.toUpperCase() (fallback earthen); #key-ind shown when levelState.hasKey; #objective-label = 'FIND THE GOLDEN KEY' (#fde047) while levelState.locked && !hasKey, else 'REACH THE PORTAL' (#c4b5fd); active brews append ' · SWIFT'/' · BERSERK' to the objective when player.fxTimers.speed/berserk > 0 (timers in frames: potions set 900, shrines 1400; speed=×1.45 move, berserk=×1.6 damage per lines 2099/2183); and calls updateBuffChips() (defined at 5095, renders countdown chips in seconds = ceil(timer/60)). levelState = {depth, biome, locked, hasKey} declared at 545.
**REC:** Merge selectively. We already have depth via the levelChanged event ('D'+depth in #wave-num) and banners; adopt their persistent depth/biome/objective text line and the buff-chip strip as Hud additions fed by EventBus events (our potion/status system from Brewing.ts + statuses supplies the chip data instead of fxTimers). SKIP the golden-key objective: a key-locked portal is a scripted door, which violates our DESIGN commandment ('no scripted doors' — we gate descent with breakable wells and physics locks). If their lock content is ported at all, recast the 'key' as a physical object the sim can deliver.

### Main loop: gamePaused modal gate + five new per-frame system hooks + minimap cadence [overlaps-ours] (L4301-4318, 4353)
step() vs original 3239-3283: (1) all gameplay updates now sit inside if(!gamePaused){...} — sim, player, enemies, particles, lightning arcs all freeze during sanctum/descend overlays (gamePaused set at 5573/6067, cleared at 6149); camera, sim-bounds, render quad transforms, bloom decay, and rendering still run while paused. (2) updateWaves() is REPLACED by updateDirector() plus four new hooks: updateMechanisms() (plates/levers/braziers/doors list at 547), updatePickups() (line 546 list), updateRuneVaults() (sealed strongrooms + rune switches, line 553), updatePortal() (portalObj line 548). (3) NEW: if(frameCount%4===0) updateMinimap() — runs every 4 frames, in BOTH modes, even while paused. HUD update stays at even-frame cadence in play mode (4353).
**REC:** Merge into Game.ts frame order carefully. We have hitstop (short gameplay freeze) but no modal pause — adopt a gamePaused-equivalent gate for shop/bench/transition overlays (our WandBench and level curtain want it). Their director/mechanisms/pickups/vaults/portal hooks correspond to systems other agents are mapping; slot ported equivalents where ctx.levels.update / waveCtl sit today (mechanisms ≈ our planned Wave-E sensor framework; portal ≈ our wells — prefer wells). Keep OUR minimap cadence (REDRAW_INTERVAL + only-when-visible M overlay) over their always-on %4 redraw, unless we add an always-on corner mini-variant.

### Build-mode held acid stream [new] (L4330-4335)
Third held-tool branch in step() (original had only dig ray and flamethrower): while buildSpellHeld && currentSpell==='acid' in build mode, calls castSpellProjectile('acid', mouse) EVERY frame — a continuous Vitriol Spray stream analogous to the flame stream.
**REC:** Port alongside the acid spell into Game.updateBuildModeHeldSpells (one extra branch, mirrors our existing flame branch).

### 8 new castable spells in castSpellProjectile (build-mode casting path) [new] (L4373-4450 (params at 504-523))
Vs original 3300-3320 (blackhole/lightning/flame/dig/default unchanged). NEW branches, with SPELL_PARAMS: (a) acid 'Vitriol Spray' {spread:0.30, mana:1.7, cd:0}: 3 ACID flying particles/cast from screen-bottom-center origin (renderCamX+VIEW_W/2, renderCamY+VIEW_H-14), speed 3.2+rand*2.2, life 30-45, grav 0.06, glow 1.4, playFlame. (b) wall 'Conjure Stone' {radius:6, range:130, mana:18, cd:24}: fills only EMPTY cells in a disc of radius 6 at target with STONE+getStoneColor, 8-particle SMOKE burst, playDig — cannot overwrite terrain/liquids. (c) embers 'Ember Storm' {count:16, mana:18, cd:30}: 16 EMBER particles, cone ±0.275rad, speed 2.6-4.8 with vy-0.8 upward bias, life 200-320, glow 2.0, grav 0.05, playFlame. (d) icelance 'Ice Lance' {velocityForce:16, damage:30, freezeFrames:150, mana:22, cd:35}: projectile type 'icelance', life 90, tone(1400,220,0.16,'sine',0.10). (e) scatter 'Scatter Shot' {pellets:5, velocityForce:9.5, spread:0.42, explosionRadius:6, mana:15, cd:20}: 5 projectiles type 'pellet', life 70, per-pellet speed ×(0.85-1.15), playZap. (f) ice 'Frost Shard' {velocityForce:11, freezeRadius:7, damage:16, mana:16, cd:16}: default branch with pType renamed to 'iceshard', life 140. (g) wisp 'Seeking Wisp' {velocityForce:4.5, damage:13, mana:8, cd:8}: life 260. (h) meteor 'Meteor Call' {velocityForce:6.5, explosionRadius:40, mana:70, cd:140}: life 300, launch vy gets extra -1.0 upward arc, playBoom(8) at cast. The 7 legacy spells' SPELL_PARAMS values are IDENTICAL to the original (no retuning). Impact behaviors for the new projectile types (pellet/iceshard/icelance/wisp/meteor — freeze application, homing, AoE) live in updateProjectiles OUTSIDE this range — consult that range's report before implementing.
**REC (superseded by current TS port status above):** These are now fixed tactical spells in Sandbox/build-mode testing, with play-mode wand/card design still the long-term combat model. Frost Shard, Ice Lance, Seeker Wisp, Ember Storm, Vitriol Spray, Conjure Stone, Meteor, and Scatter Hex should still be treated as card-candidate tuning references when they are promoted into the wand compiler. Their cast-origin (bottom-center of screen) is a build-mode quirk; play-mode wand casting uses the wand tip in our port.

### Inspector slider-range table extensions [tuning] (L4452-4512 (changes at 4476-4482))
generateContextInspector is otherwise identical to original 3323-3378 (and to our Inspector.ts). Changes: explosionRadius split out of the shared velocityForce rule into its own range 1..60 step 1 (was 1..20 step 0.5 — needed for meteor's 40 and bomb's 52, which previously rendered clamped); new key rules: pellets 2..12 step 1, freezeRadius & radius 2..16 step 1, spreadRate/meltChance/igniteChance 0..1 step 0.02. BUG: the new spell keys 'count' (Ember Storm, value 16) and 'freezeFrames' (Ice Lance, value 150) have NO range rule, so they fall through to the default min 0/max 1/step 0.05 slider — touching either slider destroys the spell's tuning.
**REC:** Adopt their range additions in src/ui/Inspector.ts (including the explosionRadius 1..60 split — ours still has the original clamped 1..20). Fix the bug on port: add 'count' (e.g. 1..40 step 1) and 'freezeFrames' (e.g. 0..600 step 10) rules, or better, make the default rule infer a range from the current value instead of 0..1.

### Worldgen trigger: regenerateWorld + biome-select wired into descent state [overlaps-ours] (L4514-4533)
Vs original 3381-3393: regenerateWorld() additionally calls resetMinimap() and sets levelPopulated=false (the flag at 554 gates one-shot level population — enemies/pickups/vaults spawn lazily on entering play, line 6600-6603). The biome-select change handler additionally writes levelState.biome=currentBiome, sets levelPopulated=false again, calls resetMinimap() (redundant double-reset) and updateHUD() so the biome label refreshes. Camera recenter on caveSpawnHint is unchanged. Adjacent context: BIOME_SEQUENCE (line 544) defines an 8-biome descent order [earthen, fungal, frozen, flooded, timber, crystal, scorched, volcanic] — 5 more biomes than the original's 3.
**REC:** Skip the trigger wiring — our Levels.ts owns regeneration, population, and minimap reset with persistence snapshots (their model regenerates destructively; ours snapshots and restores). The lazily-populated levelPopulated flag is equivalent to our placed-population-at-entry logic. The 8-biome BIOME_SEQUENCE vs our 5-level spine is a content decision for the leads: their fungal/timber/crystal/volcanic biome kits (defined at 4536+, next agent's range) could become our DESIGN-planned branch biomes rather than extending the spine.

**NOTES:** Files: NEW Y:/source/VibeCodingGames/Noita-sandbox/noita-alchemists-descent.html (range 4066-4535), ORIGINAL Y:/source/VibeCodingGames/Noita-sandbox/noita-sandbox.html (3079-3393 compared), ours: src/ui/icons.ts, src/ui/Hud.ts, src/ui/Inspector.ts, src/ui/Minimap.ts, src/game/Game.ts, src/sim/CellType.ts, src/combat/wands/cards.ts. Cross-cutting: (1) CELL-ID COLLISION is confirmed at line 4203-4210: their 21/22/23 = toxic/healium/teleportium vs our 21/22/23 = ElixirLife/Levity/Stone — every ported system touching IDs 21-30 must go through one shared remap table (their 21-30 → our 24-33), defined once before the port fleet starts. (2) Their descent model is portal+golden-key+rune-vaults (levelState.locked/hasKey, 'FIND THE GOLDEN KEY' objective) — a scripted-door design that conflicts with our DESIGN.md commandment and our well-based Levels.ts; port the CONTENT (vault rooms as card/loot sources, sanctum as Refuge shop) but not the lock model. (3) Name hazard: their 'wisp' is a SPELL (Seeking Wisp); our port already has a 'wisp' ENEMY — rename one identifier on port to avoid icon/key collisions. (4) The 7 legacy SPELL_PARAMS values are unchanged from the original — zero retuning of existing spells; everything in the spell system here is additive. (5) New-projectile impact behaviors (pellet, iceshard, icelance freeze 150 frames, wisp homing, meteor) are implemented in updateProjectiles outside this range — my numbers cover cast-time only; the port fleet must pair this report with the projectiles-range report. (6) Genuine bug found to NOT port: inspector auto-sliders render 'count' and 'freezeFrames' with default 0..1 ranges, silently destroying Ember Storm/Ice Lance tuning when touched (new file lines 4472-4499). (7) Their gamePaused freezes flying particles and lightning arcs but keeps the minimap redrawing every 4 frames even while paused and even in build mode — if we adopt a modal pause, decide deliberately what keeps ticking. (8) HUD/main-loop/inspector/build-cast scaffolding is otherwise line-identical to the original, so structural porting risk in this range is low; the value is the icon assets, the 8 new spells-as-cards, and the slider-range fixes.

## biomes-gen
### Three new biomes: Fungal Deep, Crystal Hollows, Volcanic Maw [new] (L4547-4557, 4589-4598, 4611-4621)
BIOMES registry grows from 5 (earthen/frozen/flooded/timber/scorched — values unchanged from original noita-sandbox.html:3396-3437) to 8. fungal: bands [[64,84,62],[52,72,58],[48,96,88],[88,128,104]], crown 'moss', flowerChance 0.04, pools 30 of poolElement TOXIC (cell 21 in new file), seedsOilBias 0.7, beams 30, fires 12, fungusPatches 160, healSprings 3, shrooms 26, goldBonus 1. crystal: bands [[58,66,86],[44,50,68],[78,96,120],[96,82,116]], crown 'frost', flowerChance 0.012, pools 36 of WATER, seedsOilBias 0.3, beams 0, fires 0 (no wood/fire at all), crystals 46, shrooms 40, goldBonus 1.6 (richest level). volcanic: bands [[48,40,40],[66,44,38],[96,48,34],[120,66,40]], crown 'ember', flowerChance 0, pools 160 of LAVA (vs scorched 114), seedsOilBias 0.15, beams 8, fires 110, coalSeams 90, shrooms 0, crystals 2, goldBonus 1.4. Existing biomes gained extras too: frozen +snowDrifts 90; scorched +coalSeams 60, +ashPiles 50, +shrooms 0; timber goldBonus 1.1, scorched 1.2, others 1.
**REC:** Port. fungal maps directly onto our DESIGN.md post-spine 'Fungal Gloom' branch biome; crystal and volcanic become additional branches or deep reskins. Add the new optional fields to BiomeDef in src/core/types.ts and entries in src/config/biomes.ts. Remap fungal's poolElement TOXIC=21 to a NEW appended cell ID (our 21 is ElixirLife).

### Per-biome weighted enemy tables (foes field) [overlaps-ours] (L4544, 4555, 4566, 4576, 4586, 4597, 4608, 4619)
Every biome now carries foes: [[kind, weight], ...]. earthen [[slime,5],[bat,3],[imp,2],[golem,1]]; fungal [[slime,4],[spitter,4],[bat,3],[bomber,1]]; frozen [[slime,3],[bat,4],[golem,3],[imp,1]]; flooded [[slime,5],[spitter,3],[bat,2],[golem,1]]; timber [[imp,4],[slime,3],[bomber,3],[bat,2]]; crystal [[bat,5],[golem,3],[imp,2],[bomber,1]]; scorched [[imp,5],[bomber,3],[golem,2],[bat,2]]; volcanic [[imp,5],[bomber,4],[golem,3],[bat,1]]. Consumed at new file line 2946 by level population (fallback [['slime',5],['imp',3],['golem',1]]) and at 5816 where 'heavies' (golem/spitter/imp) are filtered for key-vault guard spawns pushed into guardSpawns. References three enemy kinds new vs the original roster (bat, spitter, bomber) — different from OUR three additions (acidslime, wisp, mage).
**REC:** Merge with our populationForDepth (src/config/worldgraph.ts): ours keys counts off depth with our enemy roster; theirs keys composition off biome. Adopt the biome-keyed weighted-table shape (put it on BiomeDef or LevelDef), keep our placement mechanics (220-cell spawn clearance, physics.entityFree checks in src/game/Levels.ts placePopulation). Enemy kind mapping (bat/spitter/bomber vs acidslime/wisp/mage) must be reconciled by whoever owns the enemy-range report.

### goldBonus gold-pocket scaling [tuning] (L4911-4913 (field at 4545 etc.))
Gold decoration pass target changes from fixed 100 pockets (original 3720) to Math.round(100 * (B.goldBonus || 1)): earthen/fungal/frozen/flooded 100, timber 110, scorched 120, volcanic 140, crystal 160. Pocket shape/placement rules unchanged (r^2<=24 blob, 85% fill, must be within 6 cells of open space, 30000 tries cap).
**REC:** Adopt their numbers — one multiplier in our CaveGenerator gold pass plus a BiomeDef field.

### Descent scaffolding hooks inside generateCaves [overlaps-ours] (L4629-4631, 4689, 4699, 4711, 4723, 5057-5061)
(a) Top-of-gen reset: arteryPoints=[], pickups=[], mechanisms=[], portalObj=null, guardSpawns=[], runeVaults=[], levelPopulated=false. (b) arteryPoints sampling: every 40th x column on ALL FOUR carved arteries pushes {x, y:floor(ty)} — a cheap polyline of guaranteed-open tunnel points later used for loot/pickup placement (5857, 5871) and nearest-artery portal lookup (5979-5981). (c) End-of-gen calls applyBiomeExtras(B, FLOOR_BAND) then buildLevelStructures(FLOOR_BAND) (portal, vaults, pedestals, mechanisms, loose loot — defined in the 5761+ range). Everything between — noise field, 5-pass CA, 4 arteries with identical sin frequencies/amps/clamps, 7 jittered vertical shafts, 18 elliptical chambers, spawn chamber r24, stalactites/stalagmites, orphan-speck strip, BFS rim-light shading, crown pass, gold/pools/seeds/beams/fires/ice/flood decorations — is byte-identical to original 3440-3863.
**REC:** Skip arteryPoints — our region graph (src/world/regions.ts extractRegionGraph, the DESIGN.md 'single placement authority') is strictly more capable (regions + adjacency + articulation points vs a sampled polyline). Our CaveGenerator.generateLevel already has the equivalent end-of-gen step (bedrock floor, exit well, waystones, secrets, cauldron). Port only the applyBiomeExtras call site, slotted before our step-5 region extraction so secrets/waystones see final cells.

### applyBiomeExtras: seven new biome decoration passes [new] (L5058 (call; body 5602-5759, outside range but defines the registry fields))
(1) Glowcap fungus colonies [B.fungusPatches, fungal=160]: single FUNGUS(27) cells in EMPTY spots 4-adjacent to WALL, y 14..FLOOR_BAND-20, attempts=count*60, lifeGrid zeroed. (2) Healing springs [B.healSprings, fungal=3]: STONE basin (floor row dx -5..5 at y+1, side pillars at dx ±5 for dy 0..-2) cupping a 9-wide 2-deep pool of HEALIUM(22); requires EMPTY-over-WALL floor, avoids spawn ±60x50, 4000 attempts. (3) Mana crystal clusters [count = B.crystals ?? 5 — DEFAULT 5 FOR EVERY BIOME, crystal biome=46, volcanic=2]: anchor EMPTY cell with 4-adjacent WALL; 2-4 spikes; each spike starts jittered (x±2.5, y±1.5), length 2-5 cells growing AWAY from the anchor wall (px = sx - anchorDx*l), 50% chance of +1x widening per segment, stops at non-EMPTY; CRYSTAL(26); 14000 tries. (4) Glowshroom patches [count = B.shrooms ?? 6 — DEFAULT 6 EVERY BIOME; fungal 26, crystal 40, scorched/volcanic 0]: floor caps: EMPTY with WALL below, half-width 1-2, each qualifying column gets GLOWSHROOM(30), inner columns 60% chance second-row cap above; 14000 tries. (5) Coal seams [B.coalSeams, scorched 60, volcanic 90]: random-walk veins inside rock: start on WALL, 8-21 steps, heading drifts ±0.35 rad/step, x+=cos(a), y+=sin(a)*0.6, each step converts a 3-tall WALL column to COAL(25) at 80%; attempts=count*40. (6) Ash piles [B.ashPiles, scorched 50]: triangular drifts on EMPTY-over-WALL floors, half-width 3-7, height round((1-|dx|/w)*3) max 3, ASH(29); attempts=count*80. (7) Snow drifts [B.snowDrifts, frozen 90]: same shape but on ANY isSolid support (so they sit on ICE crusts too), half-width 4-9, height max 4 min 1, SNOW(24); attempts=count*80.
**REC:** Port as a new decorations module in src/world/, driven by the new BiomeDef fields, but route randomness through our seeded RNG (theirs uses raw Math.random — violates our determinism mandate). Behavior templates: FUNGUS/GLOWSHROOM/CRYSTAL/COAL/SNOW/ASH cell behaviors live in other ranges' reports; this pass only stamps them at gen time.

### New cell IDs consumed by generation — ABI remap required [new] (L4551 (TOXIC pool), 4593 (WATER pool), plus extras fields; IDs declared at new file 462-466)
Generation now writes cell IDs that do not exist in the original (which topped out at EMBER=20): TOXIC=21 (fungal pools), HEALIUM=22 (healing springs), SNOW=24, COAL=25, CRYSTAL=26, FUNGUS=27, ASH=29, GLOWSHROOM=30 (TELEPORTIUM=23 and GLASS=28 are not placed by gen in this range). Our Cell enum (src/sim/CellType.ts) already occupies 21/22/23 with ElixirLife/ElixirLevity/ElixirStone — the new file's TOXIC/HEALIUM/TELEPORTIUM collide exactly there. Crystal Hollows also reuses WATER as its pool element (only biome to do so).
**REC:** Append-only remap: assign Toxic, Healium(or merge), Teleportium, Snow, Coal, Crystal, Fungus, Glass, Ash, Glowshroom to IDs 24+ in src/sim/CellType.ts and bump CELL_COUNT. Decide explicitly whether HEALIUM merges into our ElixirLife (both are pink healing liquids; ours is cauldron-brewed) or stays a distinct world-placed spring liquid — recommend merge to avoid two heal-liquid IDs. TOXIC/ASH/GLASS align with DESIGN.md's planned year-one materials (Toxic Sludge, Ash, Glass).

### Fortress button handler [cosmetic] (L5064-5076)
Byte-identical to original 3865-3877 (wood pillars + gunpowder core + wall floor/roof stamp at camera center).
**REC:** Skip — already ported as src/world/fortress.ts.

**NOTES:** Hazards and cross-cutting observations: (1) LATENT BUG in the pool decoration pass (new file 4948, inherited shape from original 3755): pool color is `poolType === LAVA ? getLavaColor() : getNitrogenColor()` — so fungal's TOXIC pools and crystal's WATER pools are all painted with the NITROGEN color at gen time. When porting, pick the color factory by actual poolElement. (2) The `?? 5` / `?? 6` defaults in the crystal and glowshroom passes mean ALL FIVE LEGACY BIOMES silently gain ~5 mana-crystal clusters and ~6 glowshroom patches each — a visual/gameplay change to earthen/frozen/flooded/timber/scorched relative to both the original and our current port; decide whether to keep that (probably yes — 'mana wells in the dark' supports wand play) or zero them per-biome. (3) The entire core generator still uses unseeded Math.random(); our port runs the generator off expeditionSeed ^ FNV(levelId) (src/game/Levels.ts:194, CaveGenerator this.rng) — port all new passes through the seeded RNG or snapshots/determinism break. (4) Cell-ID collision is the single most dangerous port trap in this range: new-file 21/22/23 (TOXIC/HEALIUM/TELEPORTIUM) overlap our ElixirLife/Levity/Stone, and the new file's own 24-30 (SNOW..GLOWSHROOM) will shift when we append remapped IDs — build one translation table and use it everywhere the fleet ports cell writes. (5) applyBiomeExtras (5602-5759) and buildLevelStructures (5761+) bodies live OUTSIDE this range — I read applyBiomeExtras to document the registry fields it consumes; the 5079+/5600+ range owner should cross-check my pass descriptions and owns buildLevelStructures (portal/vaults/pedestals/mechanisms), which overlaps our exit-well/waystone/secret/cauldron generation. (6) arteryPoints is the new file's poor-man's placement authority; our region graph supersedes it — do not port it, but note their loot/portal systems (other ranges) reference it and must be re-targeted to region-graph queries. (7) The foes tables reference enemy kinds bat/spitter/bomber that neither the original nor our port has — coordinate with the enemy-range report for stat blocks before wiring biome populations. Key files: Y:/source/VibeCodingGames/Noita-sandbox/noita-alchemists-descent.html (4535-5078), Y:/source/VibeCodingGames/Noita-sandbox/noita-sandbox.html (3396-3877), Y:/source/VibeCodingGames/Noita-sandbox/src/config/biomes.ts, src/config/worldgraph.ts, src/sim/CellType.ts, src/world/CaveGenerator.ts, src/game/Levels.ts.

## descent-1
### Toast notification system (showToast) [cosmetic] (L5082-5091)
DOM toast queue in #toasts: appends a .toast div, auto-removes after 3300ms (wall-clock setTimeout), caps the stack at 5 by evicting the oldest. Used by every descent system in this range (brazier lit, rune struck, key found, chest gold, potion labels, tome learned).
**REC:** Skip the direct-DOM implementation; route as an EventBus event ('toast', {msg}) consumed by our ui/Hud.ts, which already owns banner presentation. Keep the 5-deep / 3.3s policy.

### Buff chip HUD (updateBuffChips) [cosmetic] (L5093-5106)
Renders #buff-chips html only when changed (string cache _chipCache). Shows player.buffs {stone:'STONESKIN', swift:'SWIFTNESS', levit:'LEVITATION', torch:'TORCHLIGHT'} with ceil(frames/60)s countdowns, plus player.fxTimers.speed ('SWIFT BREW') and fxTimers.berserk ('BERSERK') from cauldron brews, plus a timer-less 'CRYSTAL RESONANCE' chip when player._nearCrystal.
**REC:** Merge into our ui/Hud.ts via events once the buff timers land in entities/status.ts (see Potions entry). Our EntityStatus already drives HUD-able timers (regen/levity/stoneskin).

### Mechanism framework: metal doors, pressure plates, levers, fire braziers [new] (L5108-5299)
Single `mechanisms` array of 4 kinds, all driven by updateMechanisms (play mode only, gamePaused-gated). DOOR (makeDoor/setDoorCells 5113-5151): a w*h rect of METAL cells written into the grid (rune-tinted color [96,108,142]*0.85-1.15 via hash2 so sealed doors read as mechanisms); opening converts METAL->EMPTY with 20% spark particles; closing writes METAL but SKIPS any cell overlapping the player (|X-px|<=5, Y in py-18..py+1) or an enemy AABB+1 — anti-crush, but skipped cells stay open until the next toggle (permanent gap hazard). PLATE (makePlate 5153 + sensePlate 5201-5221): a w-wide brass METAL sill ([148,132,70]) flush with the floor; weight = count of non-EMPTY/non-gas/non-FIRE cells in the 2 rows above the sill across its width, +4 if the player stands on it (py in m.y-3..m.y+1, x overlap +-4), +4 per enemy (def.halfW overlap); pressed when weight>=3 — so ~3 cells of poured sand/water/blood from a flask trigger it (real sim sensing). Pressed sets latch=420 frames (~7s grace after weight lifts); door open while pressed||latch>0; 140Hz square press tone, door-grind SFX on state change. LEVER (makeLever 5166 + flipLever/interactLever 5185-5198): toggled by interact when player center (x,y-9) is within 22px of the handle (x,y-3); door tracks m.on every frame. BRAZIER (makeBrazier 5172 + update 5242-5267): 5-wide stone cup + 2 rim posts; while unlit, scans bowl zone x+-1, y-1..y-3 every frame — a single FIRE/LAVA/EMBER cell latches lit PERMANENTLY (toast 'A BRAZIER ROARS TO LIFE', 12 fire particles, opens its door); while lit, reseeds a FIRE cell (life 18-39) at a random empty bowl spot every 6 frames and force-opens its door. drawMechanisms (5272-5299): lever arm tilts with state + green/red glowing knob, pressed plates pulse gold along the sill, unlit braziers blink a faint ember hint every 40 frames.
**REC:** Port as the foundation of our DESIGN Wave E sensor framework — their PLATE is our PRESSURE sensor and BRAZIER is our BURN-FUSE, already cell-sampled (grid-honest). Adapt to flat typed arrays + Ctx; emit door state via events. ADD our mandated fail-open rule (theirs has none — a vault whose plate gets buried under a door's own debris, or a brazier bowl filled with water forever, can soft-lock; DESIGN requires destroyed/failed mechanisms to groan open after 30s). Fix the safe-close gap bug by retrying skipped cells on later frames. Note doors are METAL (explosion/acid-proof in both codebases) so the door itself can't be cheesed — intentional, keep.

### Rune Vaults: remote-switch strongrooms (strikeRunes/updateRuneVaults/drawRunes) [new] (L5301-5349)
`runeVaults` entries hold a floating rune glyph at (rx,ry) and a v.door array of [x,y] STONE cell coords (populated by buildTreasureVault at 5996, next range). strikeRunes(x,y,radius): any inactive vault whose glyph is within radius becomes active (toast, 220Hz+330Hz tones, screenShake +0.012 capped 0.05, 18 green particles). Callers wire it to ALL player offense: triggerExplosion passes radius+4 (L919), every non-hostile projectile impact passes radius 7 (L1785), the dig beam passes radius 7 every 6th ray step (L2370) — 'strike the glyph with any spell, blast, or dig beam'. updateRuneVaults dissolves the stone door 3 cells/frame in v.door pop order (built bottom-up), converting only cells still STONE (player may have pre-dug them) to EMPTY with green debris particles; 520Hz completion tone. drawRunes: 5-pixel glyph pulsing violet (inactive, sin period ~90 frames phase-offset by rx) or steady green (active).
**REC:** Port, but reconcile with our world/secrets.ts which fills the same niche differently (ours: sealed hollows behind breachable material skins + probeHollow audio tells — pure material gating; theirs: a scripted remote switch). They complement: keep ours as the common secret, port rune vaults as the rarer 'relic vault' tier. The glyph is the one non-grid element (violates 'the grid explains it') — consider reflavoring the strike as our planned CHARGE-LATCH sensor, or accept as the single scripted exception. Door-dissolve already writes real cells, so the payout side is grid-honest.

### Pickup entity framework (addPickup/updatePickups/drawPickups) [new] (L5351-5395, 5469-5522)
First item-entity system in the codebase (original had only homing gold particles). pickups[] entries {x,y,kind,data,bob,age,vx,vy}; kinds: key, heart, flask{flaskType}, tome{spellKey}, chest, potion{potionKind}. Physics: gravity 0.18/frame, sub-stepped vertical fall (ceil|vy| steps) settling on blocksEntity terrain (land friction vx*=0.6), horizontal motion at 0.4*vx with air drag 0.92, floor clamp at HEIGHT-4. 'pop' spawn option gives loot a tumble (vx +-0.8, vy -1.8..-2.8) — used by chest contents. Collection: distSq(p.x-px, (p.y-4)-(py-9)) < 14^2 auto-collects (no interact key). No despawn timer — pickups persist until collected or level regen clears pickups[] (L4630/6660). drawPickups: hand-pixeled bobbing sprites (sin bob amp 1.4, freq 0.06) — key, heart, corked flask tinted by FLASK_TYPES.tint, tome with glowing sigil, chest with pulsing clasp, potion bottle in POTION_DEFS colors. Update gated to play mode, alive, unpaused.
**REC:** Port the framework as src/game/Pickups.ts: it is the delivery mechanism for hearts/chests/potions/cards and our DESIGN's nest-chest and vault-chest rewards. Use ctx.physics for the terrain tests, EventBus for collection toasts. Keep the 14px magnet radius and pop physics numbers.

### Heart Containers (permanent max-HP progression) [new] (L5404-5409 (collect), spawn sites 5453 + 5828-5849 (next range))
Collecting a heart: player.maxHp += 20 and heals +40 (capped). Sources: guaranteed 1 hidden heart pocket per level (sealed rock shell with gold-fleck tell, built at 5828 — next range) and 12% chance from chests. With the endless depth loop this is unbounded HP growth (~+20/level).
**REC:** Port the pickup and numbers, but with our finite 5-level descent cap it naturally at ~6-7 hearts/expedition. Decide whether maxHp persists across expeditions (their run resets on death; our expedition model would put hearts in the within-expedition bucket).

### Spell Tomes (in-world spell unlocks) [overlaps-ours] (L5425-5437 (collect), pedestal placement 5851-5867 (next range))
Their play-mode progression: player starts with START_SPELLS=['bolt','bomb','flame','dig'] of 15 total (ALL_SPELLS L521 — 8 new spells vs the original's 7: scatter, embers, ice, icelance, wisp, wall, meteor + originals). A tome pickup carries data=spellKey; if unknown, pushes to player.spells, auto-selects it, plays learn SFX, rebuilds hotbar; if duplicate, sells for +80 gold. 1-2 tomes spawn per level on stone pedestals along arteries.
**REC:** Do NOT port tome-learning as-is — we deliberately replaced the fixed-spell hotbar with the wand/spell-card system (src/combat/wands/*, DESIGN pillar 6: cards found in world enter the deck and persist into the cross-expedition shop pool). Convert: tome pickup -> spell-card pickup feeding WandSystem/WandBench, keep the pedestal placement and the duplicate->80 gold fallback (fits our card economy).

### Treasure Chests (loot containers) [new] (L5438-5453)
On collect: gold = 60 + rand(0..79) + depth*10 added straight to globalScore (the 14 gold particles are cosmetic, grav 0.12 life 240); 1 potion guaranteed + 50% a second (random of 6 kinds); 40% a random typed flask; 12% a heart container. All contents pop-spawn as physical pickups.
**REC:** Port with numbers; chests are exactly DESIGN pillar 9's 'nest-clear and vault chests replace wave-clear rewards'. Swap the potion drops for our brewed-elixir flasks or card drops where appropriate; keep depth-scaled gold.

### Instant potions + timed buff system (POTION_DEFS + player.buffs) [overlaps-ours] (L5353-5361 (defs), 5454-5465 (effects); buff consumers at 2110/2182/2237-2240/3741 (other ranges))
Six potion kinds, instant-use on touch (not inventoried): health +35 HP; mana = full refill; stone -> buffs.stone=720 frames (12s) — damagePlayer multiplies incoming damage by 0.4 (L2110); swift -> buffs.swift=720 (12s) — faster ground movement (L2182) and jump impulse -4.1 vs -3.7 (L2237); levit -> buffs.levit=600 (10s) — hover WITHOUT draining the levitation meter (L2238-2240); torch -> buffs.torch=1500 (25s) — enlarged light radius in the renderer (L3741). Each POTION_DEFS entry also carries c1/c2 sprite colors and a light tint (potion pickups glow on the ground).
**REC:** Merge with our entities/status.ts EntityStatus timers (DESIGN pillar 5: 'a potion is a timed rewrite of entity-vs-cell rules'). Mapping: stone -> our existing stoneskin, levit -> our existing levity, health/mana -> instant effects, swift + torch -> APPEND two new timers to EntityStatus (swift's no-meter-levitation differs from ours — keep our levity semantics, adopt their durations). Keep them as world pickups for immediacy, but the brewed versions should remain our cauldron elixirs (cell IDs 21-23). Their numbers (0.4 damage multiplier, 720/720/600/1500 frames) are good starting tuning.

### Exit Portal + lock-and-key + beginDescent level transition [overlaps-ours] (L5524-5599 (+ key collect 5398-5403))
Their descent loop, the heart of this range. portalObj {x,y,open,age} placed on a stone dais by buildLevelStructures (5796, next range); open at gen time iff !levelState.locked. levelState.locked = depth>=2 && 60% chance; locked portals draw gold bars + keyhole and need the golden key pickup (placed in a guarded open vault — 'the guards ARE the lock': 3 forced guardSpawns, consumed at populateLevel L2979). Key collect: hasKey=true, portalObj.open=true. updatePortal: walking within 13px of the portal core (x, y-10) auto-triggers beginDescent — pause game, portal SFX, fade overlay, then after 650ms wall-clock: depth++, biome = BIOME_SEQUENCE[(depth-1) % 8] (8-biome cycle: earthen, fungal, frozen, flooded, timber, crystal, scorched, volcanic — endless), reroll locked, clear ALL transient arrays (enemies/projectiles/particles/arcs/shockwaves/blackhole), regenerate caves IN PLACE (the old level is DESTROYED — no persistence, no re-ascent), resetMinimap, respawn player at findSpawnPoint with invuln 90, mana+levit refilled but HP NOT healed, levelPopulated=false (enemy population deferred to main loop L6600), lift fade, then openSanctum() — their between-depths rest/shop overlay (L6066, next range) opens on ARRIVAL at the new depth.
**REC:** Ours is better on the core architecture — keep src/game/Levels.ts: persistent live Worlds per level, well-fall transitions through a diggable stone plug (the exit is itself a sim lock, vs their walk-into-portal + scripted key), finite 5-level stack, waystone respawn anchors, expedition seeding. ADOPT from theirs: (1) the lock-and-key variant as an optional level modifier — a guarded key that must be carried to the well adds a second objective our plug lacks; (2) sanctum-on-arrival pacing (our DESIGN's Refuge — theirs proves the flow); (3) depth-scaled rewards (+depth*10 chest gold); (4) their 3 extra biomes (fungal/crystal/volcanic) as post-spine branch biomes per DESIGN. Do NOT adopt: endless modulo-cycling depth, level destruction on transition, or full mana refill trivializing resource pressure between floors.

### applyBiomeExtras: 7 new biome decoration generators + 7 new materials in worldgen [new] (L5601-5759)
Called at generateCaves tail (5058) in ALL modes, before buildLevelStructures (5061). Places SEVEN new cell types (IDs at L462-466, params L490-499): (1) Glowcap FUNGUS (27) colonies — B.fungusPatches single cells (fungal biome: 160) on EMPTY orthogonally adjacent to WALL; material spreads later (spreadRate 0.10, flammability 0.50). (2) Healing springs — B.healSprings (fungal: 3): 11-wide stone basin with 3-high side walls cupping a 9x2 pool of HEALIUM (22), >=60/50 cells from spawn; HEALIUM is their healing liquid (flowRate 0.55, evaporation 0.004). (3) Mana CRYSTAL (26) clusters — count B.crystals ?? 5 so EVERY biome gets some (crystal biome: 46, volcanic: 2); anchor on a WALL face, grow 2-4 spikes of length 2-5 outward with 50% lateral thickening; gameplay hook: standing within a 17x19 scan box of any CRYSTAL cell (checked every 6 frames, L2252-2268) raises mana regen from 0.45 to 1.3/frame ('CRYSTAL RESONANCE'). (4) GLOWSHROOM (30) patches — count B.shrooms ?? 6 (fungal 26, crystal 40, scorched/volcanic 0): 3-5 wide bioluminescent caps on floors, 60% double-height. (5) COAL (25) seams — B.coalSeams (scorched 60, volcanic 90): random-walk veins 8-21 steps long, heading wander +-0.35 rad, 3-cell vertical brush converting WALL->COAL at 80%; coal is slow long fuel (burnDuration 170, igniteChance 0.08) — dig-and-ignite hazard/resource. (6) ASH (29) piles — B.ashPiles (scorched 50): triangular drifts half-width 3-7, peak 3, friction 0.20 powder. (7) SNOW (24) drifts — B.snowDrifts (frozen 90): banks half-width 4-9, peak 4 on any solid, meltChance 0.45. Also present in the file but not placed here: GLASS (28), TOXIC (21), TELEPORTIUM (23).
**REC:** Port the placement pass into CaveGenerator (slot it between decorations and our generateLevel dressing) and the biome counts into config/biomes.ts. CRITICAL cell-ID remap is now fixed by the current TS port: Toxic=24, Healium=25, Teleportium=26, Snow=27, Coal=28, Crystal=29, Fungus=30, Glass=31, Ash=32, Glowshroom=33, Moss=34. Every grid literal in ported generation code must go through that table. FUNGUS/CRYSTAL placement should request positions from our region graph instead of blind rejection sampling. ASH, GLASS, and TOXIC are already on our DESIGN year-one material list — adopt their implementations as the reference.

**NOTES:** SCOPE: lines 5078-5760 are entirely NEW vs the original — noita-sandbox.html has zero matches for toast/mechanism/pickup/portal/sanctum/buffs; the original's only meta-structure was the wave system. Everything here is the revised file's own descent layer, so every classification is against OUR TS port, not the original. BOUNDARIES: my range ends mid-section — carveGridDisc/findGroundAt/buildLevelStructures (5762+: portal chamber, key vault, treasure vaults via buildTreasureVault 5996, hidden heart pocket, tome pedestals, loose loot), openSanctum (6066, their holy-mountain rest stop with what appears to be a perks shop — player.perks.feather/manafont are referenced at L2240/2253), the minimap (6174), and the flask belt (FLASK_TYPES 6266: 10 typed flasks incl. swift/berserk brews with weighted drops, 3 belt slots) all belong to the next range; collectPickup in MY range depends on all of them. ARCHITECTURAL CONTRAST (the big one): their descent is a non-persistent endless loop — beginDescent regenerates the single world in place, destroying the level behind you, with biome cycling modulo 8 and a 60% lock-and-key gate; ours (src/game/Levels.ts) is a finite persistent 5-level stack with live World instances, well-fall transitions through diggable plugs, fire-lit waystone checkpoints, and expedition-stable seeding. These are incompatible at the transition layer — port their CONTENT (mechanisms, vaults, pickups, biome extras) into our SHELL. Their brazier (instant latch, 1 frame of fire) vs our waystone (30 hot checks at 1/4-frame cadence ≈ 8s sustained fire + full-heal checkpoint) are different systems serving different purposes (door switch vs checkpoint) — keep both, they coexist. HAZARDS: (1) cell-ID ABI collision on 21-23 (their TOXIC/HEALIUM/TELEPORTIUM vs our three elixirs) — remap table required on every ported grid literal; their 24-30 append cleanly. (2) Wall-clock setTimeouts everywhere (toast 3300ms, beginDescent 650ms, rune tones 240ms) — desync under pause; our port uses frame-based timers + the curtain pattern. (3) setDoorCells safe-close leaves permanent holes where bodies stood (cells skipped, never retried). (4) Their mechanisms lack our DESIGN-mandated fail-open rule — soft-lock audit needed when porting. (5) gamePaused is a NEW global freeze gate (sanctum/descend overlays) threaded through every update function in this range — our port has no equivalent; the curtain + transitioning flag covers transitions but not a menu-freeze; porting the sanctum will need a pause concept. (6) updateMechanisms/sensePlate iterate all enemies per plate per frame — fine at their scale, but our placed populations (45-70/level) times multiple plates warrants the region-graph proximity filter. (7) Pickups never despawn and are cleared by direct array reassignment at 4 different sites (4630, 5586 area, 6660) — in our port, pickup lists must live on LevelRuntime so they persist per-level like enemies do. SURPRISE: plates sense raw cells (3 cells of any non-gas weight trigger), so flask pours, blood, and enemy corpse-liquids legitimately solve plate puzzles — this is exactly our DESIGN pillar 8 'enemy blood conducts' philosophy already working; preserve it exactly when porting.

## descent-2
### Level-structure carve helpers: carveGridDisc / findGroundAt [overlaps-ours] (L5762-5779)
carveGridDisc(cx,cy,r): clears a disc to EMPTY (type/color/life) clamped to y in [2, HEIGHT-3]. findGroundAt(x,fromY): walks down from max(6,fromY) to HEIGHT-3 looking for an EMPTY cell with isSolid below; returns floor y or -1. Pure utilities used by every landmark placer in this range.
**REC:** Trivial; our CaveGenerator/secrets already have equivalent carve/ground-probe code. Port only as private helpers of whatever module adopts the landmark placers; thread our seeded Rng instead of Math.random in callers.

### buildLevelStructures: per-level landmark orchestrator (portal chamber, key vault, treasure vaults, heart pocket, tome pedestals, loose loot, scattered potions/chests, rune vaults) [new] (L5781-5975 (helpers connectToArtery 5977-5994, buildTreasureVault 5996-6034))
Called from generateCaves (line 5061, outside range) with FLOOR_BAND (default HEIGHT-52). Stamps 8 landmark families, each artery-connected via connectToArtery (drunken walk carving r=6 discs toward nearest arteryPoint, 80% directional bias, 900-step guard). (1) PORTAL CHAMBER: placed on the horizontal side opposite spawn (x in [WIDTH-240,WIDTH-120] or [120,240]), y = HEIGHT*(0.55..0.80); r=22 carve + 19-wide 2-row STONE dais; portalObj={x,y:py+13, open:!levelState.locked, age:0}. Touch radius 13 while open triggers beginDescent (5525-5531, other range). (2) KEY VAULT, only when levelState.locked (depth>=2 AND 60% roll set in beginDescent): kx = spawnX +- WIDTH*(0.30..0.45) clamped [80,WIDTH-80], forced >=240 from portal else pushed 320 past it; ky=HEIGHT*(0.25..0.65); r=16 chamber, 7-wide stone pedestal, 'key' pickup; NO door — 3 guardSpawns are the lock: one heavy from the biome's foes filtered to golem/spitter/imp (fallback golem) at kx-26, complementary golem/imp at kx+26, bat at (kx,ky-10). guardSpawns consumed by populateLevelEnemies (2979-2980). Collecting the key (5398-5403) sets levelState.hasKey and opens the portal remotely. (3) TREASURE VAULTS x2: vx rejection-sampled (12 tries) >=220 from spawn and >=160 from portal, vy=HEIGHT*(0.30..0.72); r=13 room + stone floor row; chest pickup, 40% bonus heart-or-flask; entry corridor (16 sine-wobble r=5 discs) on a random side sealed by makeDoor(4 wide x 12 tall METAL); mechanism = (idx + rand01)%3 selecting pressure plate (w=7), lever, or brazier placed 22 cells beyond the door on found ground; mechanisms are real-cell devices defined at 5108-5183 (other range). (4) HIDDEN HEART POCKET: random spot, r=7 carve + 'heart' pickup (+20 maxHp), re-sealed with a WALL annulus shell (49 < d2 <= 81) in brown rock; 16% GOLD flecks on the outer skin (d2>72) as the dig-here tell. (5) TOME PEDESTALS: 1-2 (second 50%) only while unknown spells remain in ALL_SPELLS (15 spells); placed near random artery points on 5-wide stone pedestals; pickup kind 'tome' data=spellKey; collecting learns the spell + selects it, duplicate auto-sells for 80 gold. (6) LOOSE LOOT: 2 random flasks + 1 chest on artery ground points. (7) SCATTER: up to 13 potions (6 POTION_KINDS) and 4 chests at open-floor spots away from spawn hint (9000-try caps). (8) RUNE VAULTS x3 (12000-try cap): requires a 25x19 probe >=90% WALL with zero METAL overlap; stamps a 23x17 METAL box (edge where |dx|>9 or |dy|>6), hollow interior; the left wall strip (2 wide x 11 tall) becomes a STONE door recorded as doorCells; loot = chest + potion + 50% heart + up to 14 loose GOLD cells; a 5-wide METAL rune-switch pedestal is placed 70-240 Manhattan-distance away (open cell with WALL below). Striking the rune (strikeRunes 5303: any explosion radius+4, any non-hostile projectile impact r7, dig beam every 6th step r7) dissolves the stone door bottom-up 3 cells/frame.
**REC:** Port the landmark CONTENT (key-guard vault, treasure-vault mechanism puzzles, heart pocket with gold-fleck tell, rune vaults) into our Levels/region-graph placement authority — request placements from extractRegionGraph instead of rejection sampling, and thread the seeded Rng (theirs is all Math.random, violating our determinism mandate). The heart pocket and rune vault are grid-expressed and fit our one-commandment; merge them with our stampSecrets archetypes. SKIP the golden-key boolean gate as-is (item-flag gate, not grid-expressed) or re-express it physically; our wells/waystones remain the descent spine. Remap guard kinds to our roster (we also have acidslime/wisp/mage).

### randomTeleport (Teleportium effect) [new] (L6036-6050)
Sets player.tpCool=120 frames (decremented at 2177; TELEPORTIUM cell contact re-triggers only when tpCool<=0, see 2218-2219), violet 18-particle burst at origin, then up to 60 attempts at a uniformly random spot (x in [20,WIDTH-40], y in [24,HEIGHT-60]) passing entityFree(tx,ty,4,17); zeroes velocity, burst + 660->1320Hz sine at destination. If all 60 attempts fail it silently does nothing (cooldown still consumed). Invoked by drinking a teleportium flask and by touching TELEPORTIUM liquid cells.
**REC:** Port as-is into our Player/status layer, but TELEPORTIUM=23 in the new file COLLIDES with our Cell.ElixirStone=23 — the liquid must be appended as a new cell ID >=24 in our append-only ABI. Consider biasing the destination sampler through the region graph so teleports never land in sealed secrets.

### Sanctum perk system (10 one-time boons) [new] (L6053-6064 (consumption sites verified at 2099-2253, 2626/2659, 6258))
PERKS array of one-shot boons stored as flags in player.perks, offered 3-at-a-time between depths. Verified mechanics: vitality +30 maxHp + full heal; manafont mana regen 0.45/f x1.6; feather levitation drain 1.15/f x0.55; blast player explosion damage x0.4; pyro fire/lava damage x0.4 (incl. hazard DoT: FIRE 0.22, LAVA 0.62 per tick); toxicology acid/toxic x0.25 (hazard ACID 0.32, TOXIC 0.20 per tick); vampirism +2 HP per kill; goldsense key always drawn on minimap; swift move speed x1.18 (stacks multiplicatively with swift-brew x1.45 and swift-potion x1.5); power all spell damage x1.25 (stacks with berserk x1.6 => x2.0 max).
**REC:** Adopt — we have no run-scoped boon system and it fills the Refuge shell (Wave B placeholder). Port the 10 effects with their exact numbers as status-engine modifiers (entities/status.ts) and player damage-source multipliers; replace goldsense's key-dot with a marker hook in our Minimap. Gate selection through our Refuge instead of a depth-transition modal.

### openSanctum: between-depths pause + boon draft UI [overlaps-ours] (L6066-6106)
Sets gamePaused, fills #sanctum-overlay DOM: shows depth + gold; offers 3 random NOT-owned perks as clickable cards; the DESCEND button is disabled with text 'CHOOSE A BOON TO DESCEND' until one perk is taken (instant apply + playLearn + visual fade of the others); when every perk is already owned the draft is skipped and descent is free. Flow inversion worth knowing: portal touch -> beginDescent (5571, other range: 650ms fade, depth++, biome=BIOME_SEQUENCE[(depth-1)%8] of ['earthen','fungal','frozen','flooded','timber','crystal','scorched','volcanic'], locked roll 60%, full cave REGENERATION destroying the old level, player respawn with invuln 90) -> openSanctum over the fresh level -> descend button -> closeSanctumAndDescend.
**REC:** Merge concept, not implementation: their descent regenerates-in-place (infinite, non-persistent, no re-ascent) which is architecturally incompatible with our persistent live-World d1-d5 Levels.ts. Hang the boon draft + shop on our well transition or Refuge as a UI-layer EventBus consumer; never let it own world regeneration.

### Sanctum shop (gold-sink between depths) [overlaps-ours] (L6108-6145)
4 repeatable items priced in globalScore gold ('oz'): MEND WOUNDS 40 (full heal), TOUGHEN UP 90 (+15 maxHp and +15 current), MYSTERY FLASK 60 (randomFlask into first empty belt slot, else overwrites selected slot), LOST PAGES 160 (learn a uniformly random unknown spell from ALL_SPELLS, auto-equip, rebuild hotbar). Buttons disable when unaffordable; shop rebuilds after each purchase so unlimited re-buys are allowed (TOUGHEN UP is an infinite HP sink).
**REC:** Merge with our Wave D card economy: LOST PAGES becomes 'buy a random spell card into the collection'; keep MEND/TOUGHEN as Refuge services with their prices as starting tuning. MYSTERY FLASK maps to a brewed-potion purchase (our Brewing.ts potions), not their typed flasks.

### closeSanctumAndDescend: level activation step [overlaps-ours] (L6147-6160)
Unpauses; in play mode: defensively builds structures if portalObj is null, then populateLevelEnemies (2958, other range: count=min(36, 12+depth*3), live cap 10+depth*2, avoids portal vicinity, spawns the key-vault guardSpawns), reveals minimap r=14 around player, banner 'DEPTH n' with objective text ('THE PORTAL IS SEALED — FIND THE GOLDEN KEY' vs 'FIND THE PORTAL'), wave horn. Related scaling outside range: enemy hp x(1+(depth-1)*0.16), damage x(1+(depth-1)*0.10) at 2575-2576.
**REC:** Ours already has placed populations via populationForDepth + POPULATION_SPAWN_CLEARANCE 220. Adopt their depth-scaling multipliers (hp +16%/depth, dmg +10%/depth) as tuning on our placed populations, and the locked/unlocked objective banner pattern as levelChanged event payload.

### Always-on corner minimap with landmark dots [overlaps-ours] (L6162-6263)
200x133 canvas at 1:8 (MM_S=8) over the 1600x1064 grid; Uint8Array explored mask; updateMinimap runs every 4th frame (4318): reveals r=8 disc around player, then incrementally recolors ONLY explored blocks within R=14 of the player by sampling the grid block-center cell through matMiniColor (hand palette: EMPTY 16/13/24, WATER 40/90/170, LAVA 220/70/20, GOLD 230/185/40, WOOD 110/74/38, ICE+SNOW 150/200/235, TOXIC 80/130/45, HEALIUM 235/120/165, CRYSTAL 110/215/235, GLOWSHROOM 130/230/150, FUNGUS 55/185/150, FIRE+EMBER 240/130/35, METAL 120/130/150, default rock 70/58/44). Overlay dots blink on frameCount%40<24: player 3x3 #e9d5ff; portal purple when open / slate when locked, drawn once its block is explored OR always at depth 1; key dot yellow when its block is explored or the goldsense perk is owned. M toggles visibility; resetMinimap on new depth; reveal r=14 burst on level entry.
**REC:** Merge with our Minimap.ts (M-overlay, frozen COLOR_FN palette, per-level explored masks in LevelRuntime, % explored caption, exit/waystone/player markers). Adopt from theirs: the always-on corner mode, incremental near-player refresh (cheaper than our full redraw), blinking quest-marker dots (portal/key -> our well exit/objectives), and conditional marker reveal rules. Keep our per-level persistence and palette approach (their per-frame matMiniColor switch is fine but our palette avoids branching).

### Flask belt: 3-slot typed consumable flasks (throw/drink), 10 types [overlaps-ours] (L6265-6425 (shatter/collision hooks at 1747, 1887-1889, 1910-1911; buffs at 2099/2183))
player.flasks=[null,null,null] + flaskSel (init 2009). FLASK_TYPES: 8 material fills mapping to cell types (water WATER, oil OIL, acid ACID, lava LAVA, nitrogen NITROGEN, toxic TOXIC, healium HEALIUM, teleportium TELEPORTIUM, each with color fn + render tint) plus 2 pure-effect brews with fill:null (swift, berserk). Weighted loot table (total 86): water 14, healium 12, oil 10, acid 9, nitrogen 8, swift 8, toxic 7, lava 6, teleportium 6, berserk 6. Controls: Q cycles to next non-empty slot (6555), RMB (6478) or F throws, V drinks, flask-bar slot click selects. THROW: consumes slot, spawns projectile {type:'flask', vx=cos(aim)*5.2+0.5*player.vx, vy=sin(aim)*5.2-1.4, life 240}, gravity 0.16/frame; shatters on terrain hit or expiry. SHATTER (6314-6334): 8 glass-shard flying particles that DEPOSIT real GLASS cells where they land (life 60-99); material flasks call splashLiquid r=4 (1958: fills EMPTY/gas cells in the disc with the cell type); effect brews burst sparkles and buff the player only if within 30px: swift fxTimers.speed=900 frames (move x1.45), berserk fxTimers.berserk=900 (spell damage x1.6). DRINK (6336-6384): healium +45 HP; teleportium -> randomTeleport; swift 1400 frames; berserk 1400; water +4 HP; lava 28 self-damage src 'fire'; acid/toxic 16 src 'toxic'; nitrogen 10 impact + cancels swift; oil no effect. Both buffs cleared on death (2158); HUD chips show remaining seconds (5102-5103). buildFlaskBar + genFlaskIconCanvas render 22x22 procedural bottle icons tinted per type.
**REC:** Mostly skip the typed-material flasks: our Flask.ts (siphon up to 600 REAL cells, rate 40/frame r8 reach 70, pour 10/frame r2, throw force 6.5 grav 0.18, conservation guaranteed) is the stronger, sim-true design and already covers carrying/throwing any liquid. ADOPT: the pure-effect brews and the drink-effect table (healium +45, swift x1.45/900f, berserk x1.6/900f, the self-damage gag rows) as cauldron Brewing.ts outputs and potion pickups; the multi-slot belt + Q-cycle UX for when we ship 3 flask slots; glass shards depositing real GLASS cells on shatter (new material). Note TOXIC=21/HEALIUM=22/TELEPORTIUM=23 in FLASK_TYPES must be remapped — those IDs are our ElixirLife/ElixirLevity/ElixirStone.

### Brush painting system (spawnCircle/drawLine) [cosmetic] (L6427-6458)
Byte-identical to the ORIGINAL noita-sandbox.html lines 3879-3910: same WALL/METAL overwrite-protection rule, SMOKE life 30-69, FIRE life 15+rand(particleLife||30), chargeGrid reset to 0, Bresenham drawLine calling spawnCircle per step. The only functional delta is contextual: COLOR_FN and the toolbar now cover cell IDs up to 30 (TOXIC 21, HEALIUM 22, TELEPORTIUM 23, SNOW 24, COAL 25, CRYSTAL 26, FUNGUS 27, GLASS 28, ASH 29, GLOWSHROOM 30), so new materials paint through the unchanged code path.
**REC:** Skip — our src/sim/brush.ts already ports this generically; new materials only need COLOR_FN entries and toolbar buttons when their cell IDs are appended to our ABI.

**NOTES:** CELL-ID HAZARD (load-bearing for every entry above): the NEW file declares TOXIC=21, HEALIUM=22, TELEPORTIUM=23, SNOW=24, COAL=25, CRYSTAL=26, FUNGUS=27, GLASS=28, ASH=29, GLOWSHROOM=30 (noita-alchemists-descent.html:462-466). Our append-only ABI already uses 21/22/23 for ElixirLife/ElixirLevity/ElixirStone, so EVERY new-file ID >=21 must use the current shared remap (24-33, plus Moss=34 and CELL_COUNT=35) — this range touches them in FLASK_TYPES, matMiniColor, shatterFlask (GLASS), and randomTeleport triggers. ARCHITECTURAL CONFLICT: their descent is an infinite regenerate-in-place loop (beginDescent destroys the old level, BIOME_SEQUENCE cycles 8 biomes, locked-portal 60% roll, no persistence/re-ascent) vs our finite persistent d1-d5 live-World stack (src/game/Levels.ts) with fire-lit waystones and breakable wells; port the CONTENT (landmarks, perks, shop, minimap markers) into our transition/Refuge, never the flow. DETERMINISM: all placement in this range is raw Math.random() — our mandate routes worldgen randomness through seeded Rng; the rejection-sampling placers should be re-anchored on extractRegionGraph (our single placement authority) which also fixes their unreachable-landmark risk (connectToArtery is the only connectivity guarantee and it carves blind drunken shafts, guard-capped at 900 steps). DESIGN-COMMANDMENT check: rune vaults, mechanism doors, and the heart pocket are real-cell constructs (good fits for our secrets/sensors); the golden key -> portal.open boolean is a scripted flag gate and should be re-expressed physically or kept out. CROSS-RANGE DEPENDENCIES documented here but owned by other agents: pickups/collect effects (5351-5467: key/heart +20maxHp/flask/tome/chest 60-140+10*depth gold/potion 6 kinds with buff frames 600-1500), mechanisms (5108-5298), strikeRunes/updateRuneVaults (5303-5333), portal touch + beginDescent (5524-5599), populateLevelEnemies (2958), depth scaling hp+16%/dmg+10% per depth (2575-2576). SURPRISES: (1) sanctum perk draft BLOCKS descent until a boon is chosen (soft-forced pick) unless all 10 owned; (2) TOUGHEN UP is an unbounded repeatable +15 maxHp gold sink; (3) flask shatter buffs require player within 30px — you can buff yourself by throwing at your feet; (4) goldsense perk reaches into the minimap renderer — a perk/UI coupling to break with an event; (5) brush system is a verbatim copy of the original — zero changes at 6427.
